Create a dark-themed 3-panel AI chat dashboard for a legal professional tool 
called "Knowledge Hub". Use Figma auto layout throughout. 
Canvas size: 1440×900px desktop frame.

---

GLOBAL DESIGN SYSTEM

Color tokens:
- bg-deep: #080C14 (icon rail)
- bg-base: #0B1120 (main content, mesh gradient overlay)
- bg-surface: #0F1829 (sidebar)
- bg-elevated: #162033 (cards, input)
- bg-hover: #1C2840 (hover states)
- accent-primary: #7C5CFC (purple)
- accent-glow: rgba(124, 92, 252, 0.15) (glow shadow)
- accent-gradient-start: #7C5CFC
- accent-gradient-mid: #A78BFA
- accent-gradient-end: #60A5FA
- text-primary: #F1F5F9
- text-secondary: #94A3B8
- text-faint: #475569
- border-subtle: #1E2D45
- border-accent: rgba(124, 92, 252, 0.4)

Typography: Inter Variable font
- Display: 38px / bold / line-height 1.1 / letter-spacing -0.02em
- Heading: 20px / semibold / line-height 1.3
- Body: 15px / regular / line-height 1.6
- Caption: 12px / medium / line-height 1.4
- Label: 11px / semibold / uppercase / letter-spacing 0.08em

Background treatment for main content:
- Base fill: #0B1120
- Add mesh gradient layer on top at 60% opacity:
  radial gradient from top-left corner: #1a0f3a → transparent (600px radius)
  radial gradient from bottom-right corner: #0a1f3d → transparent (500px radius)
- Add noise texture overlay at 4% opacity (use a grain/noise fill)

---

COLUMN 1 — Floating Icon Rail

NOT a full-height bar. Use a floating vertical pill component:
- Width: 56px, height: auto (fit content), corner radius: 28px
- Position: 16px from left edge, vertically centered in viewport
- Fill: rgba(15, 24, 41, 0.7), backdrop-filter blur 24px
- Stroke: 1px border-subtle (#1E2D45)
- Drop shadow: 0 8px 32px rgba(0,0,0,0.4)
- Auto layout: vertical, align center, gap 6px, padding 12px top/bottom

Contents top to bottom:
1. App logo icon (stylized "A" star, 28×28px, white)
   Active state: rounded square bg 40×40px fill accent-primary, icon white, 
   outer glow: 0 0 16px rgba(124,92,252,0.5)
2. Divider line: 24px wide, 1px height, fill border-subtle
3. Navigation icons (6 total: folder, home, pen-edit, clipboard, folder-variant, scales-balance)
   Each: 20×20px icon, color text-secondary
   Hover state: icon color text-primary, bg rounded 8px 36×36px fill bg-hover
   Active state: icon color accent-primary, bg 36×36px fill rgba(124,92,252,0.12)
4. Spacer (flex grow)
5. Avatar circle: 36×36px, fill accent-primary, initials "AS" 13px semibold white
   Presence dot: 10px circle, fill #22C55E, stroke 2px bg-deep, bottom-right of avatar

---

COLUMN 2 — Chat History Sidebar

Frame: 260px wide, full height, fill bg-surface (#0F1829)
Right border: 1px stroke border-subtle (#1E2D45)
No left border (adjacent to icon rail)
Auto layout: vertical, align stretch

Section A — Header (72px height, auto layout horizontal, align center, 
padding 20px horizontal, space-between):
- Left stack (auto layout vertical, gap 2px):
  "Knowledge Hub" 14px semibold text-primary
  "Chat History" 12px regular text-secondary
- Right: collapse icon button 32×32px, icon color text-secondary, 
  hover bg bg-hover, radius 8px

Section B — New Chat button (padding 16px horizontal, 12px top):
Full-width button, height 44px, fill accent-primary, corner radius 12px
"＋ New Chat" 14px semibold white, icon left 16px
Hover state: fill #8B6EFD, drop shadow 0 4px 20px rgba(124,92,252,0.4)

Section C — Search bar (padding 12px horizontal, 8px vertical):
Height 36px, fill bg-elevated, corner radius 10px, stroke 1px border-subtle
Magnifier icon 16px text-faint + placeholder "Search conversations..." 13px text-faint

Section D — Conversation list (auto layout vertical, gap 0):
Time-grouped sections with sticky labels:

Group label "TODAY" — 11px uppercase semibold text-faint, 
padding 8px 16px, letter-spacing 0.08em

List items (7 total, height 52px each):
- Auto layout horizontal, align center, gap 10px, padding 0 16px
- Hover fill: bg-hover (#1C2840), transition
- Each row:
  Chat bubble icon 16px text-secondary |
  Content stack (auto layout vertical, gap 2px):
    Title: 13px medium text-primary, max-width 140px, truncate with ellipsis
    Preview: 11px regular text-faint, truncate (optional, 1 line) |
  Timestamp: 11px text-faint, right-aligned, flex-shrink 0

Items: "NDA compliance check" / 5h ago
        "Contract expiration dates" / 2h ago
        "Risk analysis PT Marina" / 1d ago (start new group "YESTERDAY")
        "Payment terms summary" / 2d ago (start new group "LAST WEEK")
        "Termination clauses" / 3d ago
        "License agreement review" / 4d ago
        "Compliance status check" / 5d ago

Section E — User profile (72px, pinned bottom, auto layout horizontal, 
align center, padding 16px, border-top 1px border-subtle):
- Avatar 36×36px circle fill #334155, initials "EG" 13px semibold text-primary
- Stack (auto layout vertical, gap 2px):
  "Erik Gunawan" 13px semibold text-primary
  "Konsultan Hukum" 11px text-secondary
- Settings icon 20×20px text-secondary, hover color text-primary, margin-left auto

---

COLUMN 3 — Main Content Area (remaining width, fill bg-base + mesh gradient)

Empty welcome state. Content group centered both axes.

HERO GROUP (auto layout vertical, align center, gap 12px):

1. Logo + greeting row (auto layout horizontal, align center, gap 14px):
   App star icon 44×44px white
   "Hi, Erik Gunawan" display text 38px bold
   Apply linear gradient fill to "Erik Gunawan" text only:
   gradient: #7C5CFC → #A78BFA → #60A5FA, left to right

2. Subtitle text: "Ask questions about your legal documents, contracts, 
   and compliance requirements"
   16px regular text-secondary (#94A3B8), text-align center, max-width 560px

INPUT CARD (margin-top 32px):
Frame: width 820px, min-height 130px, fill bg-elevated (#162033)
Corner radius: 20px
Stroke: 1px border-subtle, hover stroke 1px border-accent 
Drop shadow idle: 0 4px 24px rgba(0,0,0,0.3)
Drop shadow focused: 0 0 0 1px border-accent, 0 0 40px accent-glow
Padding: 20px all sides

Input area: placeholder "Apa pertanyaan anda saat ini?" 15px text-faint, 
min-height 50px

Bottom toolbar (auto layout horizontal, align center, space-between, margin-top 12px):
LEFT group (auto layout horizontal, gap 8px):
- Attachment button (+): 36×36px, fill bg-surface, radius 10px, 
  icon 18px text-secondary, hover icon text-primary, hover fill bg-hover
- Document button: same size/treatment, document-text icon

RIGHT group (auto layout horizontal, gap 10px, align center):
- "Legal AI v1.0" pill: height 28px, padding 0 12px, stroke 1px border-subtle,
  radius 20px, 12px text-faint, subtle pulse animation (opacity loop 1→0.5→1, 3s)
- Voice button: 36×36px, fill bg-surface, radius 10px, mic icon 18px text-secondary,
  hover icon accent-primary, hover fill rgba(124,92,252,0.1)
- Send button: 36×36px, fill accent-primary, radius 10px, paper-plane icon 18px white
  Hover: fill #8B6EFD, drop shadow 0 4px 16px rgba(124,92,252,0.5)

QUICK-ACTION BENTO GRID (margin-top 20px, width 820px):
Use asymmetric 2-row bento layout, NOT equal 4-card row:

Row 1 (auto layout horizontal, gap 12px, height 72px):
- Card A "Pembuatan Dokumen" — width 340px
  Fill bg-elevated, radius 16px, stroke 1px border-subtle
  Auto layout horizontal, align center, gap 12px, padding 0 20px
  Icon: file-plus 22×22px, fill #7C5CFC (with 10px glow behind)
  Title: "Pembuatan Dokumen" 14px semibold text-primary
  Subtitle: "Draft NDAs, kontrak & perjanjian" 12px text-faint
  Hover: stroke 1px border-accent, shadow 0 0 24px accent-glow

- Card B "Perbandingan Dokumen" — width: fill remaining (~456px)
  Fill bg-elevated, radius 16px, stroke 1px border-subtle
  Icon: git-compare 22×22px fill #22D3EE (cyan tint)
  Title + subtitle same structure
  Subtitle: "Bandingkan versi & temukan perbedaan"
  Hover: stroke 1px rgba(34, 211, 238, 0.4), shadow cyan glow

Row 2 (auto layout horizontal, gap 12px, height 72px):
- Card C "Kepatuhan Dokumen" — width: fill (~456px)
  Icon: shield-check 22×22px fill #34D399 (green tint)
  Subtitle: "Cek regulasi & persyaratan hukum"
  Hover: stroke 1px rgba(52, 211, 153, 0.4), shadow green glow

- Card D "Analisis Kontrak" — width 340px
  Icon: search-document 22×22px fill #F59E0B (amber tint)
  Subtitle: "Identifikasi risiko & klausul kritis"
  Hover: stroke 1px rgba(245, 158, 11, 0.4), shadow amber glow

---

COMPONENT STATES TO INCLUDE:
- Icon rail icons: default, hover, active
- Sidebar conversation rows: default, hover, active/selected
- Input card: idle, focused
- Quick-action cards: default, hover
- New Chat button: default, hover
- Send / action buttons: default, hover

SPACING SYSTEM: 4px base unit — all gaps and padding must be multiples of 4px
CORNER RADIUS SCALE: 8 / 10 / 12 / 16 / 20 / 28px