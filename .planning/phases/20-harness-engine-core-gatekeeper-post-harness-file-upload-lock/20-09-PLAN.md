---
phase: 20-harness-engine-core-gatekeeper-post-harness-file-upload-lock
plan: 09
type: execute
wave: 5
depends_on: [04, 05]
files_modified:
  - frontend/src/hooks/useChatState.ts
  - frontend/src/contexts/ChatContext.tsx
  - frontend/src/components/chat/HarnessBanner.tsx
  - frontend/src/components/chat/HarnessBanner.test.tsx
  - frontend/src/pages/ChatPage.tsx
  - frontend/src/hooks/__tests__/useChatState.test.ts
autonomous: true
requirements:
  - HARN-09  # SSE event consumption (frontend half)
  - PANEL-02 # B2 fix — PANEL-02 explicitly assigned here AND Plan 20-08; the visual
             # phase progression (pending → in_progress → completed) is consumed via
             # the existing Phase 17 todos_updated reducer (already wired in this hook).
             # The PANEL-02 contract test lives in Plan 20-08 (PlanPanel.test.tsx), but
             # this plan owns the SSE arm wiring side of the requirement.

must_haves:
  truths:
    - "useChatState exposes harnessRun slice (typed null | object) initialized to null."
    - "Reducer cases handle harness_phase_start (sets running + currentPhase + phaseName), harness_phase_complete (bumps currentPhase), harness_phase_error (sets cancelled or failed by code), harness_complete (sets completed)."
    - "3000ms terminal-state-then-null timer matches AgentStatusChip's auto-fade convention."
    - "Thread switch resets harnessRun to null + refetches via GET /threads/{id}/harness/active."
    - "Reject-while-active toast: when apiFetch returns 409 with {error: 'harness_in_progress'} → dispatch toast using t('harness.reject.toast') with phase fraction injected."
    - "HarnessBanner renders when harnessRun.status active; surface bg-background border-b border-border/50 px-4 py-2 (NO blur)."
    - "HarnessBanner shows pulsing primary dot + title (running) OR static icon + terminal copy (cancelled/failed); auto-unmounts 3000ms after terminal."
    - "HarnessBanner Cancel button reuses same Dialog flow as PlanPanel (POST /threads/{id}/harness/cancel)."
    - "ChatPage slots HarnessBanner above the AgentStatusChip sticky position."
    - "W8 fix: gatekeeper_complete reducer arm SEEDS harnessRun.phaseCount = payload.phase_count (the field added by Plan 20-04 to the gatekeeper_complete SSE event payload). Eliminates the prior fallback fetch."
    - "PANEL-02 (B2): the existing todos_updated reducer arm (Phase 17) is the channel that drives the PlanPanel's phase-progression rendering. This plan does not need a new arm — the engine in Plan 20-03 emits todos_updated using the same shape. Plan 20-08 owns the visual test."
  artifacts:
    - path: "frontend/src/hooks/useChatState.ts"
      provides: "harnessRun slice + reducer cases + thread-switch reset + reject-toast dispatch + W8 phase_count seeding"
      contains: "harnessRun"
    - path: "frontend/src/components/chat/HarnessBanner.tsx"
      provides: "Sticky banner component with active + terminal states"
      contains: "data-testid=\"harness-banner\""
    - path: "frontend/src/components/chat/HarnessBanner.test.tsx"
      provides: "Vitest 3.2 co-located test covering 6 cases per UI-SPEC L319"
      contains: "harness-banner"
  key_links:
    - from: "useChatState SSE handler"
      to: "harnessRun setter"
      via: "harness_phase_* event arms"
      pattern: "harness_phase_(start|complete|error)"
    - from: "ChatPage"
      to: "HarnessBanner"
      via: "render slot above AgentStatusChip"
      pattern: "<HarnessBanner"
    - from: "useChatState gatekeeper_complete arm"
      to: "harnessRun.phaseCount initial value"
      via: "W8 — payload.phase_count seeds slice without extra fetch"
      pattern: "phase_count"
---

<objective>
Wire the backend's `harness_*` SSE events into frontend state and render the HarnessBanner that surfaces phase progression. Mirror Phase 19's `agentStatus` slice pattern exactly.

Purpose: Make the harness engine visible to the user (banner shows phase 3 of 8) and reload-safe (refetch via `/harness/active`). Provides the `harnessRun` slice that Plan 20-08 PlanPanel reads with full type safety (W10 — Plan 20-08 now depends on Plan 20-09).
Output: Extended useChatState + new HarnessBanner component + ChatPage slot + 2 pytest modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-harness-engine-core-gatekeeper-post-harness-file-upload-lock/20-CONTEXT.md
@.planning/phases/20-harness-engine-core-gatekeeper-post-harness-file-upload-lock/20-PATTERNS.md
@.planning/phases/20-harness-engine-core-gatekeeper-post-harness-file-upload-lock/20-UI-SPEC.md
@CLAUDE.md
@frontend/src/hooks/useChatState.ts
@frontend/src/contexts/ChatContext.tsx
@frontend/src/components/chat/AgentStatusChip.tsx
@frontend/src/pages/ChatPage.tsx
@frontend/src/components/ui/button.tsx
@frontend/src/components/ui/dialog.tsx
@frontend/src/lib/api.ts
@frontend/src/i18n/translations.ts

<interfaces>
<!-- Pattern source: agentStatus slice + AgentStatusChip terminal-fade -->

From frontend/src/hooks/useChatState.ts (Phase 19 patterns):
  - useState slice declaration pattern (lines 92-95)
  - thread-switch reset useEffect pattern (lines 153-158)
  - SSE-event reducer arm pattern (lines 391-425)

From frontend/src/components/chat/AgentStatusChip.tsx:
  - 3000ms terminal-state-then-null setTimeout pattern (lines 65-75)
  - role="status" aria-live="polite" sr-only fallback

From Plan 20-04 backend SSE event contract (W8 — phase_count added to gatekeeper_complete):
  harness_phase_start: { type, harness_run_id, phase_index, phase_name, phase_type }
  harness_phase_complete: { type, harness_run_id, phase_index, phase_name }
  harness_phase_error: { type, harness_run_id, phase_index, code, detail, reason? }   # reason='cancelled_by_user' for B3 cross-request cancel
  harness_complete: { type, harness_run_id, status }   # status: 'completed' | 'failed' | 'cancelled'
  harness_sub_agent_start: { type, harness_run_id, phase_index, phase_name, task_id }       # B1 — Phase 20 emits; reducer can no-op for v1.3
  harness_sub_agent_complete: { type, harness_run_id, phase_index, phase_name, task_id, status, result_summary }  # B1 — Phase 20 emits; reducer can no-op for v1.3
  gatekeeper_complete: { type, triggered, harness_run_id, phase_count, ... }  # W8 — phase_count NEW
  todos_updated: { type, todos } — already handled by Phase 17 (PANEL-02 channel)

GET /threads/{id}/harness/active response shape (Plan 20-04):
  { harnessRun: null | { id, harnessType, status, currentPhase, phaseCount, phaseName, errorDetail } }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend useChatState with harnessRun slice + reducer arms + thread-switch reset + tests (W8: seed phase_count from gatekeeper_complete)</name>
  <files>
    frontend/src/hooks/useChatState.ts,
    frontend/src/contexts/ChatContext.tsx,
    frontend/src/hooks/__tests__/useChatState.test.ts
  </files>
  <read_first>
    - frontend/src/hooks/useChatState.ts FULL FILE (lines 92-95 slice declarations, 153-158 thread-switch reset, 391-425 reducer arms)
    - frontend/src/contexts/ChatContext.tsx FULL FILE (provider exposes hooks state — need to add harnessRun + setHarnessRun to provider value)
    - frontend/src/components/chat/AgentStatusChip.tsx (3000ms terminal-fade pattern)
    - .planning/phases/20-harness-engine-core-gatekeeper-post-harness-file-upload-lock/20-UI-SPEC.md lines 262-286 (state machine spec)
    - frontend/src/lib/api.ts (apiFetch signature)
    - frontend/src/hooks/__tests__/ (existing test structure if any — co-located vs __tests__ folder convention)
  </read_first>
  <behavior>
    - Test 1: initial harnessRun is null.
    - Test 2: harness_phase_start event → harnessRun.status='running', currentPhase=event.phase_index, phaseName=event.phase_name, harnessType from prior gatekeeper_complete or fetch.
    - Test 3: harness_phase_complete event → currentPhase incremented (or stays — UI-SPEC L283 says bump).
    - Test 4: harness_phase_error with code='cancelled' → status='cancelled'.
    - Test 5: harness_phase_error with other code → status='failed', errorDetail=event.detail.
    - Test 6: harness_complete with status='completed' → status='completed'.
    - Test 7: 3000ms after status becomes terminal → harnessRun set to null (use vi.useFakeTimers).
    - Test 8: Thread switch (activeThreadId change) → harnessRun reset to null + apiFetch called for /threads/{newId}/harness/active.
    - Test 9: apiFetch 409 response with {error: 'harness_in_progress', harness_type, current_phase, phase_name} → toast dispatched with t('harness.reject.toast', {harnessType, n, m}); message draft NOT cleared.
    - Test 10: gatekeeper_complete event with triggered=true → seeds harnessRun with {id: harness_run_id, status: 'pending', currentPhase: 0, phaseCount: payload.phase_count, phaseName: ''} (W8 — phaseCount seeded directly from event payload, no fetch needed).
    - Test 11 (W8): gatekeeper_complete event payload includes phase_count → harnessRun.phaseCount = phase_count exactly. With phase_count=3 in payload, assert harnessRun.phaseCount === 3 immediately (no async fetch dependency).
    - Test 12 (B1 forward-compat): harness_sub_agent_start and harness_sub_agent_complete events arrive → reducer DOES NOT throw (v1.3 may no-op these; future Phase 21 wiring will display in PlanPanel). Assert no state mutation that changes harnessRun shape unexpectedly.
  </behavior>
  <action>
    PART A — Extend `frontend/src/hooks/useChatState.ts`:

    Add a typed slice near the existing slice declarations (around lines 92-95):

      // Phase 20 / D-01, D-02, D-09 — harness run state mirrored from backend SSE events.
      type HarnessRunSlice = null | {
        id: string
        harnessType: string
        status: 'pending' | 'running' | 'paused' | 'completed' | 'cancelled' | 'failed'
        currentPhase: number
        phaseCount: number
        phaseName: string
        errorDetail: string | null
      }
      const [harnessRun, setHarnessRun] = useState<HarnessRunSlice>(null)
      const ACTIVE_HARNESS_STATUSES = new Set(['pending', 'running', 'paused'])
      const TERMINAL_HARNESS_STATUSES = new Set(['completed', 'cancelled', 'failed'])

    Add a 3000ms terminal-fade useEffect (mirror AgentStatusChip pattern; do this in useChatState rather than the component so the slice is the single source of truth — UI-SPEC L283):

      useEffect(() => {
        if (harnessRun &amp;&amp; TERMINAL_HARNESS_STATUSES.has(harnessRun.status)) {
          const t = setTimeout(() => setHarnessRun(null), 3000)
          return () => clearTimeout(t)
        }
      }, [harnessRun])

    Add thread-switch reset + rehydration (extend existing useEffect at ~lines 153-158):

      useEffect(() => {
        // Existing resets (agentStatus, tasks) — preserve
        setHarnessRun(null)
        if (!activeThreadId) return
        apiFetch<{ harnessRun: HarnessRunSlice }>(`/threads/${activeThreadId}/harness/active`)
          .then((r) => setHarnessRun(r.harnessRun ?? null))
          .catch(() => { /* non-fatal */ })
      }, [activeThreadId])

    Add SSE event arms in the reducer block (~lines 391-425). Insert after the existing arms:

      } else if (event.type === 'harness_phase_start') {
        setHarnessRun((prev) => ({
          id: event.harness_run_id,
          harnessType: prev?.harnessType ?? '',     // gatekeeper_complete or fetch fills this
          status: 'running',
          currentPhase: event.phase_index,
          phaseCount: prev?.phaseCount ?? 0,        // W8: typically already seeded by gatekeeper_complete arm
          phaseName: event.phase_name,
          errorDetail: null,
        }))
      } else if (event.type === 'harness_phase_complete') {
        setHarnessRun((prev) => prev ? { ...prev, currentPhase: prev.currentPhase + 1 } : prev)
      } else if (event.type === 'harness_phase_error') {
        const isCancelled = event.code === 'cancelled' || event.reason === 'cancelled_by_user'
        setHarnessRun((prev) => prev ? {
          ...prev,
          status: isCancelled ? 'cancelled' : 'failed',
          errorDetail: isCancelled ? null : (event.detail ?? null),
        } : prev)
      } else if (event.type === 'harness_complete') {
        setHarnessRun((prev) => prev ? {
          ...prev,
          status: (event.status as HarnessRunSlice extends infer T ? T extends { status: infer S } ? S : never : never) ?? 'completed',
        } : prev)
      } else if (event.type === 'gatekeeper_complete' &amp;&amp; event.triggered &amp;&amp; event.harness_run_id) {
        // W8 fix: seed phaseCount directly from event payload (Plan 20-04 added this).
        // Eliminates the prior fallback fetch — banner appears with correct fraction
        // ("phase 1 of N") on first SSE tick after sentinel fires.
        const runId = event.harness_run_id as string
        const phaseCount = typeof event.phase_count === 'number' ? event.phase_count : 0
        setHarnessRun({
          id: runId,
          harnessType: '',
          status: 'pending',
          currentPhase: 0,
          phaseCount,
          phaseName: '',
          errorDetail: null,
        })
        // Optional: still fetch /harness/active to fill harnessType + phaseName,
        // since gatekeeper_complete doesn't include those (would bloat payload).
        if (activeThreadId) {
          apiFetch<{ harnessRun: HarnessRunSlice }>(`/threads/${activeThreadId}/harness/active`)
            .then((r) => { if (r.harnessRun) setHarnessRun(r.harnessRun) })
            .catch(() => { /* non-fatal */ })
        }
      } else if (
        event.type === 'harness_sub_agent_start' ||
        event.type === 'harness_sub_agent_complete'
      ) {
        // B1 forward-compat: Phase 20 engine emits these around llm_agent phase
        // dispatch. v1.3 has no UI surface for sub-agent telemetry inside the
        // harness; wire as no-op here so Phase 21 can hook in without changing
        // the engine. Importantly, do NOT mutate harnessRun shape — that's owned
        // by phase_start/complete/error.
      }

    Add reject-while-active toast handler in the apiFetch error branch (where /chat POST is called). When the response is 409 with body {error: 'harness_in_progress', harness_type, current_phase, phase_name}:

      // Plan 20-09 / D-02: reject-while-active toast (do NOT clear message draft)
      if (errBody &amp;&amp; errBody.error === 'harness_in_progress') {
        const harnessLabel = t(`harness.type.${errBody.harness_type}`) || errBody.harness_type
        const total = harnessRun?.phaseCount ?? errBody.phase_count ?? 1
        toast({
          role: 'alert',
          message: t('harness.reject.toast', {
            harnessType: harnessLabel,
            n: errBody.current_phase,
            m: total,
          }),
          duration: 5000,
        })
        return  // preserve draft; do not throw further
      }

    Export `harnessRun` and `setHarnessRun` from the hook return object.

    PART B — Extend `frontend/src/contexts/ChatContext.tsx`:

    Add harnessRun + setHarnessRun to the provider value type and pass them through. If the context value uses a generated type from useChatState's return, adding to the hook automatically extends the context — verify by checking the context value spread. Plan 20-08 depends on this (W10) — type must be exported cleanly so PlanPanel can `const { harnessRun } = useChatContext()` without `as any`.

    PART C — Tests in `frontend/src/hooks/__tests__/useChatState.test.ts` (create file if it does not exist; otherwise extend):

    12 cases per the behavior list above (W8 added 1, B1 added 1). Use `renderHook` from `@testing-library/react`, `vi.useFakeTimers()` for terminal-fade test, mock apiFetch via `vi.mock('@/lib/api', () => ({ apiFetch: vi.fn() }))`.

    Run tests until GREEN:
      cd frontend &amp;&amp; npx vitest run src/hooks/__tests__/useChatState.test.ts
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit 2>&amp;1 | head -10 &amp;&amp; npx vitest run src/hooks/__tests__/useChatState.test.ts 2>&amp;1 | tee /tmp/ucs.log; grep -E "Tests.*passed\|12 passed" /tmp/ucs.log</automated>
  </verify>
  <done>
    useChatState exposes harnessRun slice with full SSE event coverage + thread-switch refetch + reject-toast handler + W8 phase_count seeding + B1 sub-agent event no-op arms; ChatContext exposes the slice with full TS typing; 12 tests pass.
  </done>
  <acceptance_criteria>
    - `cd frontend && grep -c "harnessRun" src/hooks/useChatState.ts` returns ≥10 (slice + uses)
    - `cd frontend && grep -c "harness_phase_start\|harness_phase_complete\|harness_phase_error\|harness_complete" src/hooks/useChatState.ts` returns ≥4
    - `cd frontend && grep -c "harness_in_progress" src/hooks/useChatState.ts` returns ≥1
    - `cd frontend && grep -c "ACTIVE_HARNESS_STATUSES\|TERMINAL_HARNESS_STATUSES" src/hooks/useChatState.ts` returns ≥2
    - `cd frontend && grep -c "harnessRun" src/contexts/ChatContext.tsx` returns ≥1
    - `cd frontend && grep -c "/harness/active" src/hooks/useChatState.ts` returns ≥1
    - **W8 fix:** `cd frontend && grep -c "phase_count\|phaseCount.*phase_count" src/hooks/useChatState.ts` returns ≥1 (gatekeeper_complete arm seeds phaseCount from payload)
    - **B1 forward-compat:** `cd frontend && grep -c "harness_sub_agent_start\|harness_sub_agent_complete" src/hooks/useChatState.ts` returns ≥1 (no-op arm exists so events don't fall through to default and trigger console warns)
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npx vitest run src/hooks/__tests__/useChatState.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create HarnessBanner component + co-located test</name>
  <files>
    frontend/src/components/chat/HarnessBanner.tsx,
    frontend/src/components/chat/HarnessBanner.test.tsx
  </files>
  <read_first>
    - frontend/src/components/chat/AgentStatusChip.tsx FULL FILE (closest analog — sticky chat-header surface, 3000ms terminal-fade, role/aria, status icon switch)
    - frontend/src/components/chat/CodeExecutionPanel.test.tsx (Vitest 3.2 + I18nProvider wrapper)
    - frontend/src/contexts/ChatContext.tsx (just-extended — useChatContext exposes harnessRun)
    - frontend/src/components/ui/button.tsx + dialog.tsx (Cancel button + Dialog reuse from Plan 20-08)
    - frontend/src/lib/api.ts (apiFetch signature for cancel call)
    - .planning/phases/20-harness-engine-core-gatekeeper-post-harness-file-upload-lock/20-UI-SPEC.md lines 124-141 (HarnessBanner visual contract + aria + auto-unmount)
  </read_first>
  <action>
    Create `frontend/src/components/chat/HarnessBanner.tsx` (~80 LOC):

      import { useState } from 'react'
      import { CheckCircle2, AlertCircle, XCircle, X } from 'lucide-react'
      import { useI18n } from '@/i18n/I18nContext'
      import { useChatContext } from '@/contexts/ChatContext'
      import { Button } from '@/components/ui/button'
      import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter } from '@/components/ui/dialog'
      import { apiFetch } from '@/lib/api'
      import { cn } from '@/lib/utils'

      const ACTIVE = new Set(['pending', 'running', 'paused'])

      export function HarnessBanner() {
        const { t } = useI18n()
        const { harnessRun, activeThreadId } = useChatContext()
        const [cancelOpen, setCancelOpen] = useState(false)
        const [cancelling, setCancelling] = useState(false)

        if (!harnessRun) {
          return <div role="status" aria-live="polite" className="sr-only" data-testid="harness-banner-empty" />
        }

        const harnessLabel = t(`harness.type.${harnessRun.harnessType}`) || harnessRun.harnessType
        const isActive = ACTIVE.has(harnessRun.status)
        const isCancelled = harnessRun.status === 'cancelled'
        const isFailed = harnessRun.status === 'failed'

        async function onConfirmCancel() {
          if (!activeThreadId) return
          setCancelling(true)
          try {
            await apiFetch(`/threads/${activeThreadId}/harness/cancel`, { method: 'POST' })
          } finally {
            setCancelling(false)
            setCancelOpen(false)
          }
        }

        const titleText = isActive
          ? t('harness.banner.running', {
              harnessType: harnessLabel,
              n: String(harnessRun.currentPhase + 1),
              m: String(harnessRun.phaseCount || '?'),
              phaseName: harnessRun.phaseName,
            })
          : isCancelled
          ? t('harness.banner.cancelled', { harnessType: harnessLabel })
          : isFailed
          ? t('harness.banner.failed', { harnessType: harnessLabel, detail: harnessRun.errorDetail ?? '' })
          : ''

        return (
          <div
            role="status"
            aria-live="polite"
            data-testid="harness-banner"
            className="bg-background border-b border-border/50 px-4 py-2 flex items-center gap-2"
          >
            {isActive ? (
              <span className="h-1.5 w-1.5 rounded-full bg-primary animate-pulse" aria-hidden="true" data-testid="harness-banner-pulse" />
            ) : isCancelled ? (
              <XCircle size={14} className="text-muted-foreground" aria-hidden="true" />
            ) : (
              <AlertCircle size={14} className="text-red-600 dark:text-red-400" aria-hidden="true" />
            )}
            <span className="text-sm font-semibold text-foreground tabular-nums truncate">{titleText}</span>
            <div className="flex-1" />
            {isActive &amp;&amp; (
              <Button
                type="button"
                variant="outline"
                size="sm"
                data-testid="harness-banner-cancel"
                onClick={() => setCancelOpen(true)}
                className="h-6 px-2 gap-1"
              >
                <X size={12} />
                <span>{t('common.cancel')}</span>
              </Button>
            )}

            <Dialog open={cancelOpen} onOpenChange={setCancelOpen}>
              <DialogContent>
                <DialogHeader>
                  <DialogTitle>{t('harness.cancel.confirmTitle', { harnessType: harnessLabel })}</DialogTitle>
                  <DialogDescription>{t('harness.cancel.confirmBody')}</DialogDescription>
                </DialogHeader>
                <DialogFooter>
                  <Button variant="outline" onClick={() => setCancelOpen(false)} disabled={cancelling}>
                    {t('harness.cancel.keepRunning')}
                  </Button>
                  <Button variant="destructive" onClick={onConfirmCancel} disabled={cancelling} data-testid="harness-banner-cancel-confirm">
                    {t('harness.cancel.confirmAction')}
                  </Button>
                </DialogFooter>
              </DialogContent>
            </Dialog>
          </div>
        )
      }

    Surface invariants:
      - bg-background border-b border-border/50 px-4 py-2 flex items-center gap-2
      - NO backdrop-blur (CLAUDE.md persistent-panel rule)
      - role="status" aria-live="polite"
      - The 3000ms terminal-fade is owned by useChatState's setHarnessRun(null) effect, NOT this component.

    Create co-located test `frontend/src/components/chat/HarnessBanner.test.tsx` with 6 cases per UI-SPEC L319:
      (a) renders when harnessRun.status active — assert data-testid="harness-banner" present
      (b) hidden (sr-only stub) when harnessRun=null — assert data-testid="harness-banner-empty" present, "harness-banner" NOT present
      (c) phase fraction copy interpolates correctly — harnessRun.currentPhase=2, phaseCount=8, phaseName='Gather Context' → text contains "phase 3 of 8" or "fase 3 dari 8" depending on locale
      (d) terminal state shows for 3000ms then unmounts — set status='completed', advance fake timers 3000ms, assert harnessRun reset to null (this is owned by useChatState, so the test exercises the effect via renderHook in conjunction with the banner — or simulate by manually setting status terminal and advancing timers in a wrapped harness)
      (e) Cancel triggers Dialog — click cancel button, assert Dialog visible
      (f) ID + EN locale renders parametrize correctly — switch locale, assert title text matches translation

    Use vi.mock('@/lib/api'), vi.useFakeTimers for case (d). Wrap renders with I18nProvider and a mock ChatContext provider that exposes harnessRun + activeThreadId.

    Run:
      cd frontend &amp;&amp; npx vitest run src/components/chat/HarnessBanner.test.tsx
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/components/chat/HarnessBanner.test.tsx 2>&amp;1 | tee /tmp/hb.log; grep -E "Tests.*passed\|6 passed" /tmp/hb.log</automated>
  </verify>
  <done>
    HarnessBanner.tsx renders all 4 states (active running, cancelled, failed, null); 6 test cases pass.
  </done>
  <acceptance_criteria>
    - File `frontend/src/components/chat/HarnessBanner.tsx` exists
    - `cd frontend && grep -c "data-testid=\"harness-banner\"" src/components/chat/HarnessBanner.tsx` returns ≥1
    - `cd frontend && grep -c "data-testid=\"harness-banner-empty\"" src/components/chat/HarnessBanner.tsx` returns 1
    - `cd frontend && grep -c "data-testid=\"harness-banner-cancel\"" src/components/chat/HarnessBanner.tsx` returns 1
    - `cd frontend && grep -c "role=\"status\"" src/components/chat/HarnessBanner.tsx` returns ≥1
    - `cd frontend && grep -c "aria-live=\"polite\"" src/components/chat/HarnessBanner.tsx` returns ≥1
    - `cd frontend && grep -v '^[[:space:]]*//' src/components/chat/HarnessBanner.tsx | grep -c "backdrop-blur"` returns 0 (no blur on persistent banner)
    - `cd frontend && grep -c "harness/cancel" src/components/chat/HarnessBanner.tsx` returns ≥1
    - File `frontend/src/components/chat/HarnessBanner.test.tsx` exists
    - `cd frontend && npx vitest run src/components/chat/HarnessBanner.test.tsx` exits 0 with all cases passing
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Slot HarnessBanner into ChatPage above AgentStatusChip</name>
  <files>frontend/src/pages/ChatPage.tsx</files>
  <read_first>
    - frontend/src/pages/ChatPage.tsx FULL FILE (find the AgentStatusChip slot at lines ~65-69)
    - frontend/src/components/chat/HarnessBanner.tsx (just-built)
    - .planning/phases/20-harness-engine-core-gatekeeper-post-harness-file-upload-lock/20-UI-SPEC.md lines 124-127 (slot location: above AgentStatusChip, full-width header band)
    - .planning/phases/20-harness-engine-core-gatekeeper-post-harness-file-upload-lock/20-PATTERNS.md lines 882-898 (slot decision: ChatPage.tsx, NOT global AppLayout — banner is above chat scroll region, not the global app rail)
  </read_first>
  <action>
    Edit `frontend/src/pages/ChatPage.tsx` to add a HarnessBanner slot.

    1. Add import at top:
       import { HarnessBanner } from '@/components/chat/HarnessBanner'

    2. Locate the existing sticky AgentStatusChip slot (around lines 65-69):
       {/* Phase 19 / STATUS-01 / D-26: AgentStatusChip — sticky at top of chat column. */}
       <div className="sticky top-0 z-10 flex px-4 pt-2">
         <AgentStatusChip />
       </div>

    3. Insert HarnessBanner ABOVE this block (full-width banner, NOT inside the px-4 chip column):

       {/* Phase 20 / Plan 20-09 — HarnessBanner: full-width sticky band above chat scroll region. */}
       <div className="sticky top-0 z-20 w-full">
         <HarnessBanner />
       </div>
       {/* Existing AgentStatusChip slot below — bumped to z-10 so HarnessBanner overlays cleanly */}
       <div className="sticky top-[40px] z-10 flex px-4 pt-2">
         <AgentStatusChip />
       </div>

    The exact `top-[40px]` value depends on the rendered banner height. If the banner is `py-2` with `text-sm` content, ~36-40px is correct. Planner: verify visually by running dev server briefly OR accept ~40px and let UAT tune. The banner sr-only stub when harnessRun=null collapses to 0 height so the chip's `top-[40px]` may show a gap; planner can switch to `top-0` when banner is sr-only — handle via conditional className OR accept the small gap (preferred — simpler).

    Verify TypeScript:
      cd frontend &amp;&amp; npx tsc --noEmit
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit 2>&amp;1 | head -10 &amp;&amp; grep -c "import.*HarnessBanner" src/pages/ChatPage.tsx &amp;&amp; grep -c "<HarnessBanner" src/pages/ChatPage.tsx</automated>
  </verify>
  <done>
    ChatPage imports and renders HarnessBanner above the AgentStatusChip slot; tsc passes.
  </done>
  <acceptance_criteria>
    - `cd frontend && grep -c "import.*HarnessBanner" src/pages/ChatPage.tsx` returns 1
    - `cd frontend && grep -c "<HarnessBanner" src/pages/ChatPage.tsx` returns 1
    - `cd frontend && grep -c "AgentStatusChip" src/pages/ChatPage.tsx` returns ≥1 (preserved)
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npm run lint` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend SSE → frontend reducer | trusted (same-origin, authenticated stream) |
| frontend Cancel → backend POST /harness/cancel | authenticated; backend validates ownership |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-09-01 | T (Tampering) | injected SSE event spoof | accept | SSE comes from same-origin authenticated stream; man-in-the-middle would compromise the entire session. Frontend reducer is a trust-boundary endpoint; misuse equals app compromise. |
| T-20-09-02 | I (Information disclosure) | banner reveals other-user activity | mitigate | harnessRun fetched per activeThreadId via RLS-scoped GET /harness/active; cross-tenant data unreachable. |
| T-20-09-03 | D (Denial of service) | rapid SSE storm hangs browser | accept | React batched updates absorb high-frequency events; Phase 20 emits ~1 event per phase transition (low volume). |
</threat_model>

<verification>
- All 18+ tests pass (12 useChatState + 6 HarnessBanner).
- ChatPage renders HarnessBanner above AgentStatusChip.
- tsc + lint clean.
- Banner has role="status" aria-live="polite" and no backdrop-blur.
- Reject-while-active toast does NOT clear the message draft.
- W8: phaseCount seeded from gatekeeper_complete payload (no extra fetch).
- B1: harness_sub_agent_start/complete events handled with explicit no-op arm (no console warns; v1.3 forward-compat).
- PANEL-02: existing todos_updated reducer arm continues to work for engine-emitted todos (test lives in Plan 20-08).
</verification>

<success_criteria>
1. When backend emits harness_phase_start → banner renders within next React tick with phase fraction.
2. When backend emits harness_complete → banner shows terminal state for 3000ms then disappears.
3. Reload mid-run → useChatState refetches /harness/active and banner rehydrates.
4. With harnessRun=null → banner is an empty sr-only stub (no visual chrome).
5. **W8:** Banner shows correct "phase 1 of N" fraction the moment gatekeeper_complete fires (phase_count from payload), not after a follow-up fetch.
</success_criteria>

<output>
After completion, create `.planning/phases/20-harness-engine-core-gatekeeper-post-harness-file-upload-lock/20-09-SUMMARY.md` documenting: harnessRun slice contract, SSE event arms (including W8 phase_count seeding and B1 sub-agent no-op arms), banner component shape, ChatPage slot location, 18 total new tests.
</output>
