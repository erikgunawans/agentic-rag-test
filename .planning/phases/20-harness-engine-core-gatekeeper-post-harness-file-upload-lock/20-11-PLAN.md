---
phase: 20-harness-engine-core-gatekeeper-post-harness-file-upload-lock
plan: 11
type: execute
wave: 6
depends_on: [04, 05, 06, 07, 08, 09, 10]
files_modified:
  - backend/tests/integration/test_phase20_cross_cuts.py
  - backend/tests/integration/test_phase20_e2e_smoke.py
  - backend/app/services/harness_engine.py
  - backend/app/services/gatekeeper.py
  - backend/app/services/post_harness.py
  - backend/app/services/sub_agent_loop.py
autonomous: true
requirements:
  - SEC-02
  - SEC-03
  - SEC-04
  - OBS-01
  - OBS-02
  - OBS-03

must_haves:
  truths:
    - "SEC-04 verified: every cloud-LLM call site (gatekeeper, harness_engine llm_single, post_harness, AND sub_agent_loop when invoked from llm_agent phase) wraps the payload via egress_filter using the parent ConversationRegistry."
    - "SEC-02 verified: sub_agent_loop invoked from harness_engine inherits parent_token (no fresh JWT minted)."
    - "SEC-03 verified: no provider API keys exposed to sandbox or sub-agents — keys stay in backend env vars; harness LLM calls go through openrouter_service which reads keys server-side."
    - "OBS-01 verified: harness engine writes single-writer progress.md after each phase transition; harness phases never write to that file."
    - "OBS-02 verified: every harness operation log line includes thread_id (correlation logging consistent with v1.0 redaction logging)."
    - "OBS-03 verified: LangSmith tracing covers gatekeeper + engine + post_harness via existing client wrappers (no instrumentation gaps relative to deep-mode loop)."
    - "RLS isolation verified: User A's harness_runs and uploaded files are not readable/writable by User B."
    - "Byte-identical OFF mode verified: with HARNESS_ENABLED=False, no Phase 20 code path activates; chat behavior matches v1.2 exactly (snapshot/subprocess fixture)."
    - "End-to-end smoke harness path traversed: gatekeeper → harness trigger → 2 phase dispatch → phase_results persistence → post-harness summary → harness_runs.status=completed."
    - "B2 PANEL-02 end-to-end: during smoke-echo run, the PlanPanel's todo items transition through pending → in_progress → completed states, visible in the smoke-test snapshot or UAT screenshot."
    - "B3 cross-request cancel end-to-end: a Cancel POST issued on a SEPARATE HTTP connection while the engine is mid-run halts the engine at the next phase boundary with harness_phase_error{reason='cancelled_by_user'}."
    - "B4 single-registry invariant end-to-end: across all 4 LLM call sites in one harness turn, ConversationRegistry instance identity is preserved."
  artifacts:
    - path: "backend/tests/integration/test_phase20_cross_cuts.py"
      provides: "Cross-cut verification: SEC-02..04, OBS-01..03, RLS isolation, OFF-mode byte-identical, B3 + B4"
      contains: "test_sec_04_egress_wraps_every_llm_call"
    - path: "backend/tests/integration/test_phase20_e2e_smoke.py"
      provides: "End-to-end smoke harness test exercising the full Phase 20 stack incl. B2 todo progression"
      contains: "test_smoke_harness_full_run"
  key_links:
    - from: "all 4 cloud-LLM call sites"
      to: "egress_filter"
      via: "SEC-04 invariant"
      pattern: "egress_filter"
    - from: "harness_engine llm_agent dispatch"
      to: "sub_agent_loop with parent_token"
      via: "SEC-02 JWT inheritance"
      pattern: "parent_token=token"
    - from: "Cancel endpoint POST"
      to: "engine harness_phase_error reason='cancelled_by_user'"
      via: "B3 cross-request DB poll"
      pattern: "cancelled_by_user"
---

<objective>
Verify the cross-cutting concerns (SEC-02..04, OBS-01..03) hold across the entire Phase 20 stack, and exercise the end-to-end path with the smoke harness from Plan 20-07. Patch any gaps surfaced by the verification. Adds B2 (PANEL-02 todo progression at SC-5), B3 (cross-request cancel at SC-3), and B4 (single-registry invariant at SC-4) end-to-end checks per checker feedback.

Purpose: Ship the Phase 20 verifier's success-criteria pass. Without this plan, the 6 ROADMAP success criteria remain at "should pass" rather than "verified pass." This is the only Phase 20 plan that runs after every other plan and confirms the invariants the milestone was supposed to deliver.
Output: 2 integration test modules + targeted patches if any cross-cut gap is found.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-harness-engine-core-gatekeeper-post-harness-file-upload-lock/20-CONTEXT.md
@.planning/phases/20-harness-engine-core-gatekeeper-post-harness-file-upload-lock/20-PATTERNS.md
@CLAUDE.md
@backend/app/services/harness_engine.py
@backend/app/services/gatekeeper.py
@backend/app/services/post_harness.py
@backend/app/services/sub_agent_loop.py
@backend/app/services/redaction/egress.py
@backend/app/harnesses/smoke_echo.py
@backend/app/services/harness_runs_service.py

<interfaces>
<!-- All cross-cut surfaces are already in place from earlier plans; this plan verifies them -->

SEC-04 invariant: every cloud-LLM payload routed through egress_filter using parent ConversationRegistry
SEC-02 invariant: sub_agent_loop receives parent_token explicitly
SEC-03 invariant: openrouter_service reads OPENROUTER_API_KEY from settings; never exposed via tool params or client API
OBS-01 invariant: progress.md single writer = harness_engine; harness phase executors never write to it
OBS-02 invariant: every log line includes thread_id (or harness_run_id)
OBS-03 invariant: LangSmith tracing wraps the existing openrouter_service.client; no extra hooks needed if engine reuses it
B3 invariant: harness_engine polls harness_runs.status before each phase; cross-request Cancel takes effect at next phase boundary
B4 invariant: chat.py:_get_or_build_conversation_registry builds registry once per turn; same instance flows through gatekeeper + engine + post_harness; sub_agent_loop receives it via engine's parent_redaction_registry kwarg
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-cut verification suite (SEC + OBS + RLS + OFF-mode + B3 cancel + B4 registry)</name>
  <files>backend/tests/integration/test_phase20_cross_cuts.py</files>
  <read_first>
    - backend/app/services/harness_engine.py (Plan 20-03 — count egress_filter call sites; verify B3 dual-cancel)
    - backend/app/services/gatekeeper.py (Plan 20-04 — count egress_filter call sites)
    - backend/app/services/post_harness.py (Plan 20-05 — count egress_filter call sites)
    - backend/app/services/sub_agent_loop.py (Phase 19 — already wraps egress; harness_engine reuses it for llm_agent)
    - backend/app/routers/chat.py (Plan 20-04 — _get_or_build_conversation_registry helper for B4)
    - backend/app/config.py (harness_enabled + harness_smoke_enabled flags from Plan 20-03)
    - backend/tests/services/test_harness_runs_service.py (RLS isolation pattern reference)
    - .planning/phases/20-harness-engine-core-gatekeeper-post-harness-file-upload-lock/20-CONTEXT.md lines 16-24 (cross-cut spec)
  </read_first>
  <action>
    Create `backend/tests/integration/test_phase20_cross_cuts.py` with the following test groups (~20-25 tests total — original 20 + B3 + B4 additions):

    GROUP A — SEC-04 egress filter coverage (verify every cloud-LLM call site wraps via egress_filter):

    1. `test_sec_04_gatekeeper_calls_egress_filter` — invoke run_gatekeeper with mocked openrouter + mocked egress_filter; assert egress_filter called BEFORE openrouter create.
    2. `test_sec_04_gatekeeper_blocks_on_tripped_egress` — egress_filter returns tripped=True; assert openrouter NOT called, refusal yielded, audit log emitted with action='gatekeeper_egress_blocked'.
    3. `test_sec_04_post_harness_calls_egress_filter` — same shape as test 1 but for summarize_harness_run.
    4. `test_sec_04_post_harness_blocks_on_tripped_egress` — same shape as test 2 but for summarize_harness_run.
    5. `test_sec_04_harness_engine_llm_single_calls_egress_filter` — engine dispatches an llm_single phase; assert egress_filter called before openrouter create.
    6. `test_sec_04_harness_engine_llm_single_blocks_on_tripped_egress` — egress_filter tripped → phase result is {error: 'egress_blocked', code: 'PII_EGRESS_BLOCKED'} and harness_phase_error event emitted.
    7. `test_sec_04_harness_engine_llm_agent_uses_parent_registry` — engine dispatches an llm_agent phase via sub_agent_loop; assert sub_agent_loop received parent_redaction_registry=parent ConversationRegistry (not a fresh one).

    GROUP B — SEC-02 JWT inheritance:

    8. `test_sec_02_llm_agent_phase_inherits_parent_token` — engine dispatches llm_agent; assert sub_agent_loop called with parent_token equal to the engine's caller token (no fresh JWT minted).

    GROUP C — SEC-03 provider key custody:

    9. `test_sec_03_no_api_key_in_tool_params` — confirm `openrouter_service.client` reads API key from server settings; grep across harness_engine.py + gatekeeper.py + post_harness.py + smoke_echo.py for any literal API key reference (`OPENROUTER_API_KEY` should appear ONLY in config.py / openrouter_service.py).

    GROUP D — OBS-01 single-writer progress.md:

    10. `test_obs_01_progress_md_written_after_phase_transition` — engine completes a phase; assert WorkspaceService.write_file called with file_path='progress.md' and content matches `## Phase {N}: {name}\n` pattern.
    11. `test_obs_01_harness_phase_executors_do_not_write_progress_md` — grep smoke_echo.py for `progress.md` literal — should be 0 matches (engine writes, phases don't).

    GROUP E — OBS-02 thread_id correlation logging:

    12. `test_obs_02_harness_engine_logs_include_thread_id` — capture log records during run_harness_engine; assert at least one log line contains `thread_id` token.
    13. `test_obs_02_gatekeeper_logs_include_thread_id` — same for run_gatekeeper.
    14. `test_obs_02_harness_runs_service_logs_include_thread_id` — start_run / fail / cancel log lines include thread_id (or run_id which transitively maps).

    GROUP F — OBS-03 LangSmith tracing coverage:

    15. `test_obs_03_langsmith_wraps_openrouter_calls` — verify openrouter_service.client uses the wrapped LangSmith client (existing v1.0 instrumentation). Inspect via `from app.services.openrouter_service import openrouter_service; assert hasattr(openrouter_service.client, '_langsmith_wrapped') OR openrouter_service.client.__class__.__name__ contains 'wrap_openai' artefact`. If LangSmith isn't auto-instrumented in tests (env-gated), assert the import path exists rather than runtime hook.

    GROUP G — RLS isolation (cross-tenant guard):

    16. `test_rls_user_b_cannot_read_user_a_harness_run` — using two synthetic JWTs with different user_ids, insert a harness_run as User A, assert User B's get_active_run for the same thread returns None (RLS denies). NOTE: requires actual Supabase test fixture or live test DB with RLS enabled; if not available in unit-test env, mock the supabase client to return [] when authed token's user mismatches and document as a smoke-grep verification.
    17. `test_rls_user_b_cannot_write_to_user_a_workspace_files` — same shape for register_uploaded_file.

    GROUP H — Byte-identical OFF mode (D-16 invariant):

    18. `test_off_mode_chat_endpoint_skips_harness_branches` — set settings.harness_enabled=False; POST /chat with a normal message; assert harness_runs_service.get_active_run NOT called (or called and returned None — either is acceptable as long as no Phase-20 routing branch activates).
    19. `test_off_mode_upload_endpoint_returns_404_when_workspace_disabled` — settings.workspace_enabled=False; POST /threads/{id}/files/upload; assert 404 with {error: 'workspace_disabled'}. (W6 fix: gating moved from harness_enabled to workspace_enabled per D-13.)
    20. `test_off_mode_harness_registry_is_empty_when_smoke_disabled` — settings.harness_enabled=True but harness_smoke_enabled=False; reload smoke_echo module; assert 'smoke-echo' NOT in list_harnesses().

    GROUP I — B3 cross-request cancel (NEW per checker B3 fix):

    21. **NEW (B3):** `test_cancel_from_separate_request_halts_engine_at_next_phase_boundary` — start the smoke-echo engine on connection A (consume the SSE generator manually). After phase 0 yields harness_phase_complete but BEFORE phase 1's harness_phase_start would emit, simulate a separate request hitting POST /threads/{id}/cancel-harness — which flips the harness_runs.status to 'cancelled' in the DB. Mock `harness_runs_service.get_run_by_id` so the second call (the engine's pre-phase poll) returns `{"status": "cancelled"}`. Assert the engine's next yielded event is `harness_phase_error` with `reason='cancelled_by_user'` and `phase_index=1`, followed by `harness_complete{status='cancelled'}`. The engine MUST NOT call phase 1's executor.
    22. **NEW (B3 supporting):** `test_engine_polls_harness_runs_status_before_each_phase` — drive a 3-phase smoke harness; assert `harness_runs_service.get_run_by_id` is called BEFORE each `_dispatch_phase` invocation (3 times). This pins the polling cadence.

    GROUP J — B4 single-registry invariant (NEW per checker B4 fix):

    23. **NEW (B4):** `test_b4_chat_helper_returns_same_instance_within_single_call` — call `_get_or_build_conversation_registry(thread_id, sys_settings={'pii_redaction_enabled': True})` once; assert the result is whatever `ConversationRegistry.load` returned (mocked sentinel). Pin the contract.
    24. **NEW (B4):** `test_b4_gatekeeper_engine_post_harness_share_registry` — invoke `_gatekeeper_stream_wrapper` end-to-end with all internal services mocked; capture the `registry=` kwarg argument received by `run_gatekeeper`, `run_harness_engine`, and `summarize_harness_run`; assert all three are the SAME object (object identity).
    25. **NEW (B4):** `test_b4_engine_forwards_same_registry_to_sub_agent_loop` — engine dispatches an llm_agent phase; assert `run_sub_agent_loop` was called with `parent_redaction_registry=` equal-by-identity to the registry the engine itself received.

    Run:
      cd backend &amp;&amp; source venv/bin/activate &amp;&amp; pytest tests/integration/test_phase20_cross_cuts.py -v

    If any test fails, surface the gap and patch the relevant module (e.g., add a missing thread_id field to a log line, add a missing egress_filter wrap, add a missing `get_run_by_id` poll, ...). Each patch is in-scope for this plan.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; source venv/bin/activate &amp;&amp; pytest tests/integration/test_phase20_cross_cuts.py -v 2>&amp;1 | tee /tmp/cc.log; grep -E "passed" /tmp/cc.log</automated>
  </verify>
  <done>
    All 25 cross-cut tests pass; any gaps surfaced have been patched in the relevant module.
  </done>
  <acceptance_criteria>
    - File `backend/tests/integration/test_phase20_cross_cuts.py` exists
    - `grep -c "async def test_\|def test_" backend/tests/integration/test_phase20_cross_cuts.py` returns ≥23
    - `grep -c "test_sec_04" backend/tests/integration/test_phase20_cross_cuts.py` returns ≥4 (4 SEC-04 tests minimum)
    - `grep -c "test_obs_01\|test_obs_02\|test_obs_03" backend/tests/integration/test_phase20_cross_cuts.py` returns ≥3
    - `grep -c "test_off_mode" backend/tests/integration/test_phase20_cross_cuts.py` returns ≥2
    - `grep -c "test_rls" backend/tests/integration/test_phase20_cross_cuts.py` returns ≥1
    - **B3 fix:** `grep -c "test_cancel_from_separate_request\|cancelled_by_user" backend/tests/integration/test_phase20_cross_cuts.py` returns ≥2
    - **B4 fix:** `grep -c "test_b4_\|same_registry\|object identity\|is parent_registry" backend/tests/integration/test_phase20_cross_cuts.py` returns ≥3
    - `pytest tests/integration/test_phase20_cross_cuts.py -v` exits 0 with all tests passing
    - Egress wrap grep gate (whole stack): `grep -c "egress_filter" backend/app/services/harness_engine.py backend/app/services/gatekeeper.py backend/app/services/post_harness.py` returns ≥3 (one per file minimum)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: End-to-end smoke harness integration test (success criteria #1-6, B2 PANEL-02 progression at SC-5)</name>
  <files>backend/tests/integration/test_phase20_e2e_smoke.py</files>
  <read_first>
    - backend/app/harnesses/smoke_echo.py (Plan 20-07)
    - backend/app/services/harness_engine.py (Plan 20-03 — engine entry point)
    - backend/app/services/gatekeeper.py (Plan 20-04 — sentinel detection)
    - backend/app/services/post_harness.py (Plan 20-05 — summary)
    - backend/app/services/harness_runs_service.py (Plan 20-02 — state transitions)
    - .planning/ROADMAP.md lines 132-138 (Phase 20 success criteria #1-6 verbatim)
  </read_first>
  <action>
    Create `backend/tests/integration/test_phase20_e2e_smoke.py` with high-level integration tests that exercise the full Phase 20 path with the smoke harness. Heavy mocking is acceptable (we are testing wiring, not the LLM); the goal is to confirm event sequence + state transitions + persistence.

    Test cases (each maps to one of the 6 ROADMAP success criteria):

    SC-1 (gatekeeper trigger flow):
      `test_sc1_gatekeeper_runs_when_eligible_and_triggers_via_sentinel`
        - Setup: HARNESS_ENABLED=True, HARNESS_SMOKE_ENABLED=True, registry contains smoke-echo, no active harness_runs for thread.
        - Action: invoke run_gatekeeper with a mocked openrouter that streams "Ready to begin [TRIGGER_HARNESS]".
        - Assert: gatekeeper_complete event with triggered=True, harness_runs.start_run called with name='smoke-echo'.
        - **W8 sub-assert:** the gatekeeper_complete payload includes `phase_count == len(smoke_echo.phases)`.

    SC-2 (engine dispatch + ~5k orchestrator + cancellation):
      `test_sc2_engine_dispatches_phases_in_sequence_with_cancellation_support`
        - Setup: smoke-echo HarnessDefinition + asyncio.Event() cancellation_event.
        - Action: drive run_harness_engine; halfway through (after phase 0 complete) set cancellation_event.
        - Assert: phase 0 completes, phase 1 yields harness_phase_error with code='cancelled', harness_runs.cancel called.
        - Bonus assert: engine prompt context (system prompt + workspace inputs) stays under ~5k chars in test fixtures (smoke-echo has trivial inputs — easy bound to assert).

    SC-3 (llm_single Pydantic enforcement + SSE event suite + B3 cross-request cancel):
      `test_sc3_llm_single_pydantic_validation_and_sse_event_suite`
        - Setup: smoke-echo, mock openrouter to return invalid JSON for phase 1 first, then valid on retry (but engine has NO retry — STATUS-03 — so first invalid output causes harness_phase_error).
        - Assert: harness_phase_error code='INVALID_OUTPUT' emitted; engine stops; no automatic retry happens.
        - Second sub-test: valid JSON path → assert event sequence emits harness_phase_start, harness_phase_complete, harness_complete in order.
      **NEW (B3) — SC-3 supplementary:** `test_sc3_cancel_from_separate_request_halts_engine_at_next_phase_boundary`
        - Setup: drive smoke-echo engine on a manually-stepped async generator. After phase 0 yields harness_phase_complete, mock `harness_runs_service.get_run_by_id` to return `{"status": "cancelled"}` (simulating a separate POST /cancel-harness request).
        - Assert: the next yielded event is `harness_phase_error` with `reason='cancelled_by_user'` and `phase_index=1`. The engine MUST NOT call phase 1's executor. Final event is `harness_complete{status='cancelled'}`.
        - This is the user-facing E2E proof of the B3 dual-layer cancellation. The unit-level smoke test for the same behavior lives in Plan 20-03's test file.

    SC-4 (post-harness summary inline + 30k truncation + B4 single-registry):
      `test_sc4_post_harness_summary_streams_inline_after_complete`
        - Setup: completed smoke-echo run with phase_results.
        - Action: invoke summarize_harness_run.
        - Assert: at least one delta event emitted; summary_complete event emitted with assistant_message_id set (mock returns id); messages.insert called with harness_mode='smoke-echo'.
      `test_sc4_post_harness_truncates_when_phase_results_exceed_30k`
        - Setup: phase_results with 5 fake phases, total >30k chars.
        - Action: call _truncate_phase_results.
        - Assert: phases 0..2 contain TRUNCATION_MARKER; phases 3 and 4 contain full content.
      **NEW (B4) — SC-4 supplementary:** `test_sc4_all_4_llm_call_sites_share_one_registry_instance`
        - Setup: drive `_gatekeeper_stream_wrapper` end-to-end with all four call sites mocked (gatekeeper, engine.llm_single, sub_agent_loop, post_harness). Patch `_get_or_build_conversation_registry` to return a sentinel `MagicMock(name='RegistrySentinel')`.
        - Assert: all four call sites received the SAME object via `is` identity (not equal-by-value, identity).
        - This is the user-facing E2E proof of the B4 single-registry invariant.

    SC-5 (file upload + agent_todos phase tracking + tool stripping + B2 PANEL-02 progression):
      `test_sc5_upload_endpoint_stores_binary_with_source_upload`
        - Setup: WORKSPACE_ENABLED=True; mock WorkspaceService.write_binary_file + supabase upsert.
        - Action: POST a valid PDF via TestClient.
        - Assert: 200; register_uploaded_file called with source='upload'.
      `test_sc5_engine_writes_agent_todos_with_harness_prefix`
        - Setup: drive engine; mock agent_todos_service.write_todos.
        - Assert: write_todos called with todos whose `content` starts with `[Smoke Echo]` (from smoke-echo.display_name).
      `test_sc5_panel_03_llm_agent_phase_strips_write_read_todos_from_tools`
        - Setup: dispatch a synthetic llm_agent phase whose `tools=['write_todos','read_todos','search_documents']`; mock run_sub_agent_loop.
        - Assert: run_sub_agent_loop received `tools=['search_documents']` (write_todos and read_todos filtered).
      **NEW (B2 PANEL-02) — SC-5 supplementary:** `test_sc5_engine_emits_todos_with_status_transitions_pending_in_progress_completed`
        - Setup: drive smoke-echo engine end-to-end (2 phases: validate-input, echo).
        - Action: capture every `agent_todos_service.write_todos` call.
        - Assert sequence (per phase, for the phase's todo at index N):
            (1) Initial write: todos[N].status == 'pending' (pre-phase seed).
            (2) After phase N harness_phase_start emit: todos[N].status == 'in_progress'.
            (3) After phase N harness_phase_complete emit: todos[N].status == 'completed'.
        - Assert across all phases: at the end of the run, every todo's final status is 'completed' (or remains 'pending' if engine stopped early — but smoke-echo must complete cleanly in this test).
        - This is the user-facing E2E proof of PANEL-02. The frontend visual differentiator test lives in Plan 20-08 (PlanPanel.test.tsx).

    SC-6 (cross-cuts full sweep — relies on Task 1 results):
      `test_sc6_cross_cut_invariants_all_pass`
        - This is a meta-test that imports the cross-cut test file's results OR re-runs key assertions:
          - egress_filter called from each of 3 service files (one each)
          - sub_agent_loop receives parent_token (not a fresh one)
          - progress.md written by engine, not phases
          - thread_id appears in log lines
          - HARNESS_ENABLED=False fully short-circuits
          - B3 dual-layer cancellation works (cross-request DB poll)
          - B4 single-registry invariant holds across 4 call sites

    Run:
      cd backend &amp;&amp; source venv/bin/activate &amp;&amp; pytest tests/integration/test_phase20_e2e_smoke.py -v
  </action>
  <verify>
    <automated>cd backend &amp;&amp; source venv/bin/activate &amp;&amp; pytest tests/integration/test_phase20_e2e_smoke.py -v 2>&amp;1 | tee /tmp/e2e.log; grep -E "passed" /tmp/e2e.log</automated>
  </verify>
  <done>
    All 6 success-criteria-mapped tests pass (now with B2 + B3 + B4 supplementary tests); engine + gatekeeper + post_harness + upload pipeline + RLS + tool stripping + cross-request cancel + single-registry invariant + todo progression verified end-to-end.
  </done>
  <acceptance_criteria>
    - File `backend/tests/integration/test_phase20_e2e_smoke.py` exists
    - `grep -c "async def test_sc[1-6]\|def test_sc[1-6]" backend/tests/integration/test_phase20_e2e_smoke.py` returns ≥9 (one+ per ROADMAP success criterion plus B2, B3, B4 supplementary)
    - `grep -c "smoke-echo\|smoke_echo" backend/tests/integration/test_phase20_e2e_smoke.py` returns ≥3
    - **B2 PANEL-02 fix:** `grep -c "in_progress\|pending.*in_progress.*completed\|todos.*status" backend/tests/integration/test_phase20_e2e_smoke.py` returns ≥2
    - **B3 fix:** `grep -c "cancelled_by_user\|cancel_from_separate_request" backend/tests/integration/test_phase20_e2e_smoke.py` returns ≥1
    - **B4 fix:** `grep -c "same.*registry\|RegistrySentinel\|share_one_registry\|all_4_llm_call_sites" backend/tests/integration/test_phase20_e2e_smoke.py` returns ≥1
    - `pytest tests/integration/test_phase20_e2e_smoke.py -v` exits 0
    - Combined test count: `pytest tests/integration/test_phase20_cross_cuts.py tests/integration/test_phase20_e2e_smoke.py -v` shows 30+ tests passing
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

(All Phase 20 trust boundaries already covered by Plans 20-01..20-10. This plan verifies the boundaries hold under realistic test load.)

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-11-01 | I (Information disclosure) | unverified egress wrap | mitigate | Test suite in this plan asserts egress_filter wraps every cloud-LLM call in gatekeeper, harness_engine.llm_single, post_harness, and sub_agent_loop. Any gap surfaces as a failing test, requiring patch in this plan. |
| T-20-11-02 | E (Elevation of privilege) | RLS isolation regression | mitigate | Cross-tenant tests confirm User B cannot read User A's harness_runs / uploaded files. RLS policies from Plan 20-01 enforce. |
| T-20-11-03 | R (Repudiation) | log gaps | mitigate | OBS-02 tests confirm thread_id correlation in log lines across engine, gatekeeper, harness_runs_service. |
| T-20-11-04 | T (Tampering) | OFF-mode side effects | mitigate | OFF-mode tests confirm zero Phase-20 code paths activate when HARNESS_ENABLED=False — true byte-identical behavior. |
| T-20-11-05 | D (Denial of service) | runaway harness ignoring user Cancel | mitigate | B3 cross-request cancel test (`test_cancel_from_separate_request_halts_engine_at_next_phase_boundary`) proves the engine notices a DB-status flip from a separate HTTP request and exits at the next phase boundary. |
| T-20-11-06 | I (Information disclosure) | divergent registries leak PII | mitigate | B4 single-registry invariant tests prove all 4 LLM call sites in one harness turn receive the same ConversationRegistry instance (object identity). |
</threat_model>

<verification>
- All 30+ tests pass.
- Any cross-cut gap surfaced has been patched in the relevant module (gatekeeper, engine, post_harness, sub_agent_loop, or harness_runs_service).
- Egress wrap count holds: ≥1 per service file.
- RLS cross-tenant test passes (or is documented as live-DB-required if test infra cannot enforce RLS in unit-test mode).
- OFF-mode invariant verified via 3 tests (chat short-circuit, upload 404 when workspace_enabled=False, smoke registry empty).
- B2 PANEL-02 todo progression verified end-to-end.
- B3 cross-request cancel verified end-to-end.
- B4 single-registry invariant verified end-to-end.
</verification>

<success_criteria>
1. The 6 ROADMAP success criteria are E2E verifiable via the smoke harness.
2. SEC-02..04 invariants hold across all 3 new service files.
3. OBS-01..03 invariants verified.
4. RLS isolation holds (verified or documented).
5. With HARNESS_ENABLED=False, Phase 20 is byte-identical to v1.2.
6. **B2:** PlanPanel todos visibly progress pending → in_progress → completed during smoke-echo run.
7. **B3:** Cancel from a separate HTTP request halts the engine at the next phase boundary.
8. **B4:** All 4 LLM call sites in one harness turn share one ConversationRegistry instance.
</success_criteria>

<output>
After completion, create `.planning/phases/20-harness-engine-core-gatekeeper-post-harness-file-upload-lock/20-11-SUMMARY.md` documenting: 30+ cross-cut tests, all 6 success criteria mapped to test cases, B2 + B3 + B4 supplementary E2E verifications, any gaps surfaced and patched.
</output>
