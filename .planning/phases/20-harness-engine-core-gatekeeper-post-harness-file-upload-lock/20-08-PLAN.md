---
phase: 20-harness-engine-core-gatekeeper-post-harness-file-upload-lock
plan: 08
type: execute
wave: 5
# W10 fix: 20-08 now depends on 20-09 so that the harnessRun slice is available
# from useChatContext with full TypeScript typing (no `as any` fallback). 20-09
# declares the slice; 20-08 consumes it.
depends_on: [04, 05, 09]
files_modified:
  - frontend/src/components/chat/PlanPanel.tsx
  - frontend/src/components/chat/__tests__/PlanPanel.test.tsx
  - frontend/src/i18n/translations.ts
autonomous: true
requirements:
  - PANEL-01
  - PANEL-02
  - PANEL-04

must_haves:
  truths:
    - "PlanPanel renders the locked variant header (Lock icon + harness display label + Cancel button + collapse chevron) when harnessRun.status IN ('pending','running','paused')."
    - "Lock icon (lucide Lock, 16px, text-primary) is wrapped in shim Tooltip showing t('harness.lock.tooltip')."
    - "Cancel button (outline shadcn variant + X icon + t('common.cancel')) opens shadcn Dialog with title/body/destructive-confirm/dismiss."
    - "Dialog confirm calls POST /threads/{thread_id}/harness/cancel via apiFetch."
    - "PlanPanel renders the existing (unlocked) header when harnessRun is null OR status is terminal (completed/failed/cancelled)."
    - "paused status renders identically to running (no throw — Phase 21 forward-compat per UI-SPEC L118)."
    - "Surface remains solid bg-background — NO backdrop-blur (CLAUDE.md persistent-panel rule)."
    - "common.cancel + harness.lock.tooltip + harness.cancel.* + harness.type.* keys added to BOTH id and en blocks of translations.ts (UI-SPEC copy table)."
    - "PANEL-02 (B2 fix): PlanPanel renders the harness phase todos with status differentiation (pending → in_progress → completed) when todos_updated SSE arrives during an active harness_run; verified by a Vitest co-located test that mocks the SSE event and asserts each status arm's visual differentiator."
    - "harnessRun read directly from useChatContext with full TS typing (no `as any` fallback) — depends on Plan 20-09 declaring the slice (W10 fix)."
  artifacts:
    - path: "frontend/src/components/chat/PlanPanel.tsx"
      provides: "Extended PlanPanel with locked variant + Cancel Dialog"
      contains: "harnessRun"
    - path: "frontend/src/i18n/translations.ts"
      provides: "New i18n keys for harness lock + cancel + harness type labels + common.cancel"
      contains: "harness.lock.tooltip"
  key_links:
    - from: "PlanPanel locked header"
      to: "POST /threads/{id}/harness/cancel"
      via: "Dialog confirm via apiFetch"
      pattern: "harness/cancel"
    - from: "PlanPanel locked header tooltip"
      to: "shim Tooltip render prop"
      via: "frontend/src/components/ui/tooltip.tsx asChild→render shim"
      pattern: "Tooltip"
    - from: "PlanPanel todos render"
      to: "todos_updated SSE event arms in useChatState (Phase 17 + Phase 20-03 engine emits)"
      via: "PANEL-02 — phase progression rendering during active harness_run"
      pattern: "todos_updated"
---

<objective>
Extend `PlanPanel.tsx` with the locked variant per UI-SPEC L107-117 (Lock icon, harness-type label, Cancel button + Dialog, paused-state forward compat). Add the required i18n keys (UI-SPEC copy table L168-188). Add an explicit PANEL-02 test that pins the phase-progression rendering during an active harness run (B2 fix).

Purpose: Communicate immutability to the user (PANEL-04) and provide the primary Cancel affordance (D-03). Backend-side enforcement (PANEL-03 LLM-side strip + PANEL-01 phase-as-todo) was completed in Plan 20-03. PANEL-02 (visual phase progression) is wired implicitly via the existing Phase 17 todos_updated rendering, but B2 requires an explicit test that proves the wiring still holds when the SSE source is the Phase 20 engine (not Phase 17 deep-mode).
Output: Extended PlanPanel + extended PlanPanel test + i18n additions + PANEL-02 phase-progression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-harness-engine-core-gatekeeper-post-harness-file-upload-lock/20-CONTEXT.md
@.planning/phases/20-harness-engine-core-gatekeeper-post-harness-file-upload-lock/20-PATTERNS.md
@.planning/phases/20-harness-engine-core-gatekeeper-post-harness-file-upload-lock/20-UI-SPEC.md
@CLAUDE.md
@frontend/src/components/chat/PlanPanel.tsx
@frontend/src/components/chat/__tests__/PlanPanel.test.tsx
@frontend/src/components/ui/tooltip.tsx
@frontend/src/components/ui/dialog.tsx
@frontend/src/components/ui/button.tsx
@frontend/src/i18n/translations.ts
@frontend/src/lib/api.ts

<interfaces>
<!-- All UI primitives already vendored; locked variant is composition only -->

From frontend/src/components/ui/tooltip.tsx:
  Tooltip + TooltipTrigger asChild + TooltipContent (asChild→render shim is automatic)

From frontend/src/components/ui/dialog.tsx:
  Dialog open/onOpenChange, DialogContent, DialogHeader, DialogTitle,
  DialogDescription, DialogFooter, Button outline + destructive variants

From frontend/src/lib/api.ts:
  apiFetch(path, init?) — base-URL prefix + auth header injection

harnessRun slice shape (declared by Plan 20-09 useChatState extension — Plan 20-08 depends_on=[09]):
  null OR { id: string, harnessType: string, status: 'pending'|'running'|'paused'|'completed'|'cancelled'|'failed', currentPhase: number, phaseCount: number, phaseName: string, errorDetail: string|null }

ACTIVE_HARNESS_STATUSES = new Set(['pending', 'running', 'paused'])

todos_updated SSE event shape (Phase 17 contract — Phase 20 engine emits the same shape):
  { type: 'todos_updated', todos: Array<{ content: string, status: 'pending'|'in_progress'|'completed', position: number }> }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add new i18n keys to translations.ts (id + en blocks)</name>
  <files>frontend/src/i18n/translations.ts</files>
  <read_first>
    - frontend/src/i18n/translations.ts (existing structure — find the id and en block locations)
    - .planning/phases/20-harness-engine-core-gatekeeper-post-harness-file-upload-lock/20-UI-SPEC.md lines 168-198 (full copywriting table)
  </read_first>
  <action>
    Add the following keys to BOTH the `id` and `en` translation blocks. Match the existing file's nesting/dot-notation convention.

    EN block additions (verbatim text in quotes, dot-notation keys):
      common.cancel = "Cancel"
      harness.banner.running = "{harnessType} running — phase {n} of {m} ({phaseName})"
      harness.banner.cancelled = "{harnessType} cancelled"
      harness.banner.failed = "{harnessType} failed — {detail}"
      harness.lock.tooltip = "System-driven plan — cannot be modified during execution"
      harness.cancel.confirmTitle = "Cancel {harnessType}?"
      harness.cancel.confirmBody = "Completed work will be saved to the workspace. The current phase will stop at the next checkpoint."
      harness.cancel.confirmAction = "Cancel run"
      harness.cancel.keepRunning = "Keep running"
      harness.reject.toast = "{harnessType} running (phase {n}/{m}) — please wait"
      harness.type.contract-review = "Contract Review"
      harness.type.smoke-echo = "Smoke Echo"
      chat.attachFile = "Attach file"
      chat.attachFile.tooltip = "Attach DOCX or PDF (max 25 MB)"
      upload.tooLarge = "File too large — max {max} MB"
      upload.wrongMime = "Only DOCX and PDF files are supported"
      upload.serverError = "Upload failed — please try again"
      upload.cancelled = "Upload cancelled"
      upload.inProgress = "Uploading {filename} — {percent}%"

    ID block additions (verbatim text in quotes, same dot-notation keys):
      common.cancel = "Batal"
      harness.banner.running = "{harnessType} berjalan — fase {n} dari {m} ({phaseName})"
      harness.banner.cancelled = "{harnessType} dibatalkan"
      harness.banner.failed = "{harnessType} gagal — {detail}"
      harness.lock.tooltip = "Rencana sistem — tidak dapat diubah saat berjalan"
      harness.cancel.confirmTitle = "Batalkan {harnessType}?"
      harness.cancel.confirmBody = "Pekerjaan yang sudah selesai akan disimpan di Ruang Kerja. Fase saat ini akan berhenti pada titik aman berikutnya."
      harness.cancel.confirmAction = "Hentikan"
      harness.cancel.keepRunning = "Lanjutkan"
      harness.reject.toast = "{harnessType} berjalan (fase {n}/{m}) — mohon tunggu"
      harness.type.contract-review = "Tinjauan Kontrak"
      harness.type.smoke-echo = "Smoke Echo"
      chat.attachFile = "Lampirkan berkas"
      chat.attachFile.tooltip = "Lampirkan DOCX atau PDF (maks 25 MB)"
      upload.tooLarge = "Berkas terlalu besar — maks {max} MB"
      upload.wrongMime = "Hanya berkas DOCX dan PDF yang didukung"
      upload.serverError = "Gagal mengunggah — mohon coba lagi"
      upload.cancelled = "Unggahan dibatalkan"
      upload.inProgress = "Mengunggah {filename} — {percent}%"

    Surgical-changes rule: do NOT retro-migrate existing branch.cancel, clauseLibrary.cancel, etc. (per UI-SPEC L198).

    Verify TypeScript compiles after edit.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit 2>&amp;1 | head -20 &amp;&amp; grep -c "common.cancel" src/i18n/translations.ts &amp;&amp; grep -c "harness.lock.tooltip" src/i18n/translations.ts &amp;&amp; grep -c "harness.banner.running" src/i18n/translations.ts &amp;&amp; grep -c "harness.cancel.confirmTitle" src/i18n/translations.ts</automated>
  </verify>
  <done>
    All 19 keys present in both id and en blocks; tsc passes.
  </done>
  <acceptance_criteria>
    - `cd frontend && grep -c '"common.cancel"\|common\\.cancel' src/i18n/translations.ts` returns ≥2 (id + en)
    - `cd frontend && grep -c '"harness.lock.tooltip"\|harness\\.lock\\.tooltip' src/i18n/translations.ts` returns ≥2
    - `cd frontend && grep -c '"harness.banner.running"\|harness\\.banner\\.running' src/i18n/translations.ts` returns ≥2
    - `cd frontend && grep -c '"upload.tooLarge"\|upload\\.tooLarge' src/i18n/translations.ts` returns ≥2
    - `cd frontend && grep -c '"chat.attachFile"\|chat\\.attachFile' src/i18n/translations.ts` returns ≥2
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extend PlanPanel.tsx with locked variant + Cancel Dialog (W10: full type safety, no `as any`)</name>
  <files>frontend/src/components/chat/PlanPanel.tsx</files>
  <read_first>
    - frontend/src/components/chat/PlanPanel.tsx FULL FILE (existing header at lines 88-103, surface at lines 80-86)
    - frontend/src/components/chat/AgentStatusChip.tsx FULL FILE (status-switch + 3000ms terminal-fade pattern reference)
    - frontend/src/components/ui/tooltip.tsx (shim API: asChild prop)
    - frontend/src/components/ui/dialog.tsx (Dialog composition)
    - frontend/src/components/ui/button.tsx (variant prop options)
    - frontend/src/contexts/ChatContext.tsx (Plan 20-09 has already added harnessRun to provider value — depends_on enforces ordering)
    - frontend/src/lib/api.ts (apiFetch signature)
    - .planning/phases/20-harness-engine-core-gatekeeper-post-harness-file-upload-lock/20-UI-SPEC.md lines 107-121 (locked PlanPanel visual contract)
  </read_first>
  <action>
    Modify `frontend/src/components/chat/PlanPanel.tsx`. Surgical scope: add a locked-variant code path; do NOT touch the unlocked render or list-item rendering.

    Imports to add at top:
      import { Lock, X } from 'lucide-react'
      import { Tooltip, TooltipTrigger, TooltipContent } from '@/components/ui/tooltip'
      import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter } from '@/components/ui/dialog'
      import { Button } from '@/components/ui/button'
      import { apiFetch } from '@/lib/api'
      import { useState } from 'react'  // if not already imported

    Constant near top of file:
      const ACTIVE_HARNESS_STATUSES = new Set(['pending', 'running', 'paused'])

    **W10 fix — full type safety:**
    Read harnessRun from context with FULL TypeScript typing. Plan 20-09 (which this plan now depends on per the updated frontmatter) declares:
    ```ts
    type HarnessRunSlice = null | {
      id: string
      harnessType: string
      status: 'pending'|'running'|'paused'|'completed'|'cancelled'|'failed'
      currentPhase: number
      phaseCount: number
      phaseName: string
      errorDetail: string | null
    }
    ```
    and exposes `harnessRun: HarnessRunSlice` from `useChatContext()`.

    Use:
    ```ts
    const { harnessRun, threadId } = useChatContext()
    ```
    DO NOT use any of these fallback patterns:
    ```ts
    // FORBIDDEN — these were proposed in the prior plan revision and are removed
    // by W10 fix:
    const harnessRun = (useChatContext() as any).harnessRun ?? null
    const harnessRun = useChatContext().harnessRun ?? null  // unsafe if context not yet typed
    ```
    The `depends_on=[09]` in this plan's frontmatter guarantees Plan 20-09 has landed before Plan 20-08 executes, so the typed read above will type-check cleanly.

    Add a locked-header switch above the existing header JSX. The existing header lives at lines ~88-103 and renders an `<h3>` with title + chevron. Replace it with:

      const isLocked = harnessRun != null &amp;&amp; ACTIVE_HARNESS_STATUSES.has(harnessRun.status)
      const harnessLabelKey = harnessRun ? `harness.type.${harnessRun.harnessType}` : ''
      const harnessLabel = harnessRun ? (t(harnessLabelKey) || harnessRun.harnessType) : ''
      const [cancelOpen, setCancelOpen] = useState(false)
      const [cancelling, setCancelling] = useState(false)

      async function onConfirmCancel() {
        if (!harnessRun || !threadId) return
        setCancelling(true)
        try {
          await apiFetch(`/threads/${threadId}/harness/cancel`, { method: 'POST' })
        } catch (e) {
          // toast via existing toast infra; non-fatal — UI re-renders on next SSE harness_phase_error
        } finally {
          setCancelling(false)
          setCancelOpen(false)
        }
      }

      // Header render
      <div className="flex items-center justify-between px-4 py-3 border-b border-border/50 gap-2">
        {isLocked ? (
          <>
            <Tooltip>
              <TooltipTrigger asChild>
                <Lock size={16} className="text-primary shrink-0" data-testid="harness-lock-icon" aria-label={t('harness.lock.tooltip')} />
              </TooltipTrigger>
              <TooltipContent>{t('harness.lock.tooltip')}</TooltipContent>
            </Tooltip>
            <h3 className="text-sm font-semibold text-foreground truncate">{harnessLabel}</h3>
            <div className="flex-1" />
            <Button
              type="button"
              variant="outline"
              size="sm"
              onClick={() => setCancelOpen(true)}
              data-testid="harness-cancel-button"
              className="h-8 px-3 gap-1"
            >
              <X size={14} />
              <span>{t('common.cancel')}</span>
            </Button>
            <button
              type="button"
              onClick={() => setCollapsed((c) => !c)}
              aria-label={collapsed ? 'expand' : 'collapse'}
              className="flex h-6 w-6 items-center justify-center rounded hover:bg-accent transition-colors text-muted-foreground"
            >
              {collapsed ? <ChevronRight size={14} /> : <ChevronDown size={14} />}
            </button>
          </>
        ) : (
          <>
            <h3 className="text-sm font-semibold text-foreground">{t('planPanel.title')}</h3>
            <button
              type="button"
              onClick={() => setCollapsed((c) => !c)}
              aria-label={collapsed ? 'expand' : 'collapse'}
              className="flex h-6 w-6 items-center justify-center rounded hover:bg-accent transition-colors text-muted-foreground"
            >
              {collapsed ? <ChevronRight size={14} /> : <ChevronDown size={14} />}
            </button>
          </>
        )}
      </div>

    Append the Cancel Dialog at end of component JSX (sibling to header):

      <Dialog open={cancelOpen} onOpenChange={setCancelOpen}>
        <DialogContent>
          <DialogHeader>
            <DialogTitle>{t('harness.cancel.confirmTitle').replace('{harnessType}', harnessLabel)}</DialogTitle>
            <DialogDescription>{t('harness.cancel.confirmBody')}</DialogDescription>
          </DialogHeader>
          <DialogFooter>
            <Button variant="outline" onClick={() => setCancelOpen(false)} disabled={cancelling}>
              {t('harness.cancel.keepRunning')}
            </Button>
            <Button variant="destructive" onClick={onConfirmCancel} disabled={cancelling} data-testid="harness-cancel-confirm">
              {t('harness.cancel.confirmAction')}
            </Button>
          </DialogFooter>
        </DialogContent>
      </Dialog>

    Surface invariant: the outer panel container (lines ~80-86) MUST stay `bg-background` with NO `backdrop-blur` per CLAUDE.md persistent-panel rule. Do not modify it.

    Forward-compat: the conditional `isLocked` covers `paused` because it uses ACTIVE_HARNESS_STATUSES which includes 'paused' (Phase 21 forward-compat per UI-SPEC L118).

    Run TypeScript + ESLint:
      cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit 2>&amp;1 | head -10 &amp;&amp; grep -c "harness-lock-icon" src/components/chat/PlanPanel.tsx &amp;&amp; grep -c "harness-cancel-button" src/components/chat/PlanPanel.tsx &amp;&amp; grep -c "harness/cancel" src/components/chat/PlanPanel.tsx &amp;&amp; grep -c "ACTIVE_HARNESS_STATUSES" src/components/chat/PlanPanel.tsx &amp;&amp; grep -c "backdrop-blur" src/components/chat/PlanPanel.tsx</automated>
  </verify>
  <done>
    PlanPanel.tsx has locked-variant header + Cancel Dialog; tsc passes; no backdrop-blur introduced; no `as any` typing fallback.
  </done>
  <acceptance_criteria>
    - `cd frontend && grep -c "harness-lock-icon" src/components/chat/PlanPanel.tsx` returns 1
    - `cd frontend && grep -c "harness-cancel-button" src/components/chat/PlanPanel.tsx` returns 1
    - `cd frontend && grep -c "harness-cancel-confirm" src/components/chat/PlanPanel.tsx` returns 1
    - `cd frontend && grep -c "harness/cancel" src/components/chat/PlanPanel.tsx` returns 1 (the apiFetch path)
    - `cd frontend && grep -c "ACTIVE_HARNESS_STATUSES" src/components/chat/PlanPanel.tsx` returns ≥1
    - `cd frontend && grep -v '^[[:space:]]*//' src/components/chat/PlanPanel.tsx | grep -c "backdrop-blur"` returns 0 (no new backdrop-blur in non-comment lines)
    - **W10 fix:** `cd frontend && grep -c "as any" src/components/chat/PlanPanel.tsx` returns 0 (no `as any` typing escape hatch)
    - `cd frontend && grep -c "import.*Lock.*from 'lucide-react'\|Lock,.*X.*from 'lucide-react'\|Lock.*lucide-react" src/components/chat/PlanPanel.tsx` returns ≥1
    - `cd frontend && grep -c "import.*Dialog" src/components/chat/PlanPanel.tsx` returns ≥1
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npm run lint` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Extend PlanPanel test with locked-variant cases + PANEL-02 phase-progression case (B2)</name>
  <files>frontend/src/components/chat/__tests__/PlanPanel.test.tsx</files>
  <read_first>
    - frontend/src/components/chat/__tests__/PlanPanel.test.tsx FULL FILE (existing test cases — extend rather than replace per planner-locked decision below)
    - frontend/src/components/chat/CodeExecutionPanel.test.tsx (Vitest 3.2 reference for I18nProvider + apiFetch mock pattern)
    - .planning/phases/20-harness-engine-core-gatekeeper-post-harness-file-upload-lock/20-UI-SPEC.md lines 317-322 (test-coverage contract — required cases a–e)

    PLANNER LOCKED DECISION (resolves PATTERNS.md fact-check #4): EXTEND the existing PlanPanel.test.tsx file with locked-variant cases. Reasoning: keeps unlocked + locked tests adjacent for refactor safety; PATTERNS.md L46 explicitly leans toward this; co-located convention preserved.
  </read_first>
  <action>
    Append 6 new test cases to the existing `frontend/src/components/chat/__tests__/PlanPanel.test.tsx` (5 locked-variant + 1 PANEL-02 phase-progression). Use the existing render helper (or add a thin wrapper that takes a harnessRun override).

    Required cases (per UI-SPEC L317-318):
    (a) renders locked variant when harnessRun.status='running' — assert lock icon present (data-testid="harness-lock-icon"), Cancel button visible (data-testid="harness-cancel-button"), header uses display label from t('harness.type.smoke-echo')
    (b) lock tooltip copy matches t('harness.lock.tooltip') — hover/focus the lock icon, assert TooltipContent text contains "System-driven plan" (en) or "Rencana sistem" (id)
    (c) Cancel button click opens Dialog — click harness-cancel-button, assert Dialog visible with confirmTitle ("Cancel Smoke Echo?") and destructive confirm button
    (d) paused status renders without throwing — pass harnessRun.status='paused', assert render does not throw and lock icon present
    (e) renders existing variant when harnessRun=null — assert lock icon NOT present, header shows t('planPanel.title')

    Bonus case (f): Cancel confirm calls apiFetch with POST /threads/{id}/harness/cancel — mock apiFetch, click confirm, assert apiFetch called once with correct path + method.

    **NEW (B2 PANEL-02):**
    (g) `PlanPanel renders harness todos with status arms (pending → in_progress → completed) when todos_updated SSE arrives during active harness_run`:
        - Setup: harnessRun = { status: 'running', currentPhase: 0, phaseCount: 3, ...smoke-echo defaults }.
        - Initial state: render PlanPanel with 3 todos all `status='pending'` (the engine seeds these in Plan 20-03 Task 4).
        - Assertion 1: Each todo row renders with the pending visual differentiator (existing PlanPanel.tsx pending arm — typically a circle outline icon or muted color).
        - Action 1: Simulate a `todos_updated` SSE event arriving via the mock ChatContext provider, flipping todo[0].status to 'in_progress'.
        - Assertion 2: todo[0] now renders with the in_progress differentiator (existing PlanPanel.tsx in_progress arm — typically a spinner or active color).
        - Action 2: Simulate a second `todos_updated` event flipping todo[0] to 'completed' and todo[1] to 'in_progress'.
        - Assertion 3: todo[0] renders with the completed differentiator (existing PlanPanel.tsx completed arm — typically a check icon or strikethrough); todo[1] renders with the in_progress differentiator.
        - Use the existing PlanPanel test helpers to grab the rendered todo rows (they likely use data-testid="todo-row" or similar; if not, add a stable selector).
        - This test pins the PANEL-02 contract: when the engine emits todos_updated during an active harness_run, the existing Phase 17 todos rendering machinery still works AND visually differentiates the three statuses.

    Use vi.mock('@/lib/api', () => ({ apiFetch: vi.fn() })) at module top.
    Wrap renders with I18nProvider (existing pattern in CodeExecutionPanel.test.tsx).

    Run:
      cd frontend &amp;&amp; npx vitest run src/components/chat/__tests__/PlanPanel.test.tsx
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/components/chat/__tests__/PlanPanel.test.tsx 2>&amp;1 | tee /tmp/pp.log; grep -E "Test Files.*passed\|Tests.*passed" /tmp/pp.log</automated>
  </verify>
  <done>
    PlanPanel.test.tsx contains 6+ new cases (5 locked-variant + 1 PANEL-02 phase-progression); all pass.
  </done>
  <acceptance_criteria>
    - `cd frontend && grep -c "harnessRun.*running\|status.*running" src/components/chat/__tests__/PlanPanel.test.tsx` returns ≥1
    - `cd frontend && grep -c "harness-lock-icon" src/components/chat/__tests__/PlanPanel.test.tsx` returns ≥1
    - `cd frontend && grep -c "harness-cancel-button\|harness-cancel-confirm" src/components/chat/__tests__/PlanPanel.test.tsx` returns ≥1
    - `cd frontend && grep -c "paused" src/components/chat/__tests__/PlanPanel.test.tsx` returns ≥1
    - `cd frontend && grep -c "harness/cancel" src/components/chat/__tests__/PlanPanel.test.tsx` returns ≥1
    - **B2 PANEL-02 fix:** `cd frontend && grep -c "todos_updated\|todos.*pending.*in_progress.*completed\|PANEL-02" src/components/chat/__tests__/PlanPanel.test.tsx` returns ≥1
    - **B2 PANEL-02 fix:** `cd frontend && grep -c "in_progress\|'in_progress'" src/components/chat/__tests__/PlanPanel.test.tsx` returns ≥1
    - `cd frontend && npx vitest run src/components/chat/__tests__/PlanPanel.test.tsx` exits 0 with all tests passing
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI → POST /threads/{id}/harness/cancel | authenticated; backend verifies ownership via RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-08-01 | T (Tampering) | UI bypass of lock | mitigate | UI hides mutation affordances when locked, but defense-in-depth lives in Plan 20-03 (LLM tool exclusion) and backend (Plan 20-04 cancel endpoint requires active run); UI is a last courtesy line. |
| T-20-08-02 | I (Information disclosure) | tooltip leaks harness internals | accept | Tooltip text is the localized constant — no harness-internal IDs surfaced. |
| T-20-08-03 | S (Spoofing) | spoofed harnessRun in browser devtools | accept | A user editing their own React state grants no privilege; the cancel endpoint validates the active run server-side. |
</threat_model>

<verification>
- All i18n keys present in both id and en blocks.
- PlanPanel.tsx tsc passes; lint passes.
- PlanPanel.test.tsx passes with new locked-variant cases + PANEL-02 phase-progression case (B2).
- No `backdrop-blur` introduced on the persistent panel surface.
- No `as any` typing fallback (W10).
</verification>

<success_criteria>
1. With harnessRun.status='running', the panel renders the lock icon, harness-type label, and Cancel button.
2. Clicking Cancel opens a Dialog with destructive confirm + dismiss; confirm hits POST /threads/{id}/harness/cancel.
3. With harnessRun=null, the panel looks identical to pre-Phase-20 (unlocked variant).
4. paused status does not throw — Phase 21 forward-compat verified by test.
5. **B2 PANEL-02:** During an active harness_run, todos_updated SSE events drive the panel through pending → in_progress → completed visual states, verified by the new co-located test.
</success_criteria>

<output>
After completion, create `.planning/phases/20-harness-engine-core-gatekeeper-post-harness-file-upload-lock/20-08-SUMMARY.md` documenting: 19 i18n keys added, locked PlanPanel header structure, Cancel Dialog flow, 6 new test cases (5 locked-variant + 1 PANEL-02 phase-progression), W10 type-safety improvement (depends on 20-09).
</output>
