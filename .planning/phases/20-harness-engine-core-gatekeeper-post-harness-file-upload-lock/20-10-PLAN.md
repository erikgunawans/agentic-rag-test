---
phase: 20-harness-engine-core-gatekeeper-post-harness-file-upload-lock
plan: 10
type: execute
wave: 5
depends_on: [06, 09]
files_modified:
  - frontend/src/components/chat/FileUploadButton.tsx
  - frontend/src/components/chat/FileUploadButton.test.tsx
  - frontend/src/components/chat/MessageInput.tsx
  - frontend/src/components/chat/WelcomeInput.tsx
  - frontend/src/hooks/useChatState.ts
autonomous: true
requirements:
  - UPL-04

must_haves:
  truths:
    - "FileUploadButton renders a Paperclip lucide icon (16px) in the chat input toolbar with byte-identical class string to existing Plus / FileText buttons in InputActionBar (UI-SPEC L146)."
    - "Hidden when usePublicSettings() returns workspace_enabled=false. Per CONTEXT.md D-13, paperclip is ALWAYS visible when WORKSPACE_ENABLED=true (W6 fix — corrected from prior harness_enabled gating). The paperclip works for both flows: (a) a registered harness via gatekeeper trigger, and (b) general workspace upload when no harness is registered (file lands as source='upload' with no gatekeeper trigger)."
    - "Hidden file input accepts only .docx and .pdf; client-side validation rejects size >25 MB and wrong MIME with localized toast messages."
    - "POST /threads/{id}/files/upload via apiFetch with multipart FormData; AbortController supports user cancel."
    - "Single in-flight upload at a time (sequential queue per UI-SPEC L154)."
    - "Success: file row flashes green border 200ms in WorkspacePanel — handled by WorkspacePanel via SSE workspace_updated; this plan's success path emits no extra toast."
    - "uploadingFiles slice in useChatState tracks the in-flight upload (filename, size, percent, abort controller); reducer cleans on success/error/abort."
    - "FileUploadButton inserted in BOTH MessageInput.tsx AND WelcomeInput.tsx (form-duplication rule per CLAUDE.md note)."
  artifacts:
    - path: "frontend/src/components/chat/FileUploadButton.tsx"
      provides: "Paperclip button + file picker + multipart upload + AbortController + progress + error toasts"
      contains: "Paperclip"
    - path: "frontend/src/components/chat/FileUploadButton.test.tsx"
      provides: "Vitest 3.2 coverage of 8 cases per UI-SPEC L320"
      contains: "FileUploadButton"
  key_links:
    - from: "FileUploadButton click"
      to: "POST /threads/{id}/files/upload"
      via: "apiFetch multipart FormData"
      pattern: "files/upload"
    - from: "FileUploadButton in MessageInput.tsx"
      to: "FileUploadButton in WelcomeInput.tsx"
      via: "form-duplication rule (CLAUDE.md)"
      pattern: "<FileUploadButton"
---

<objective>
Build the paperclip upload button (UPL-04) and slot it into both MessageInput and WelcomeInput. Add the `uploadingFiles` state slice to track in-flight progress.

Purpose: Complete the user-facing upload flow that lets contracts reach harness Phase-1 dispatchers (Plan 20-07 smoke harness, future Plan 22 Contract Review). Per D-13, the paperclip is also the entry point for general workspace upload when no harness is registered (W6 fix).
Output: New component + test + 2 input-host edits + useChatState slice extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-harness-engine-core-gatekeeper-post-harness-file-upload-lock/20-CONTEXT.md
@.planning/phases/20-harness-engine-core-gatekeeper-post-harness-file-upload-lock/20-PATTERNS.md
@.planning/phases/20-harness-engine-core-gatekeeper-post-harness-file-upload-lock/20-UI-SPEC.md
@CLAUDE.md
@frontend/src/components/chat/InputActionBar.tsx
@frontend/src/components/chat/MessageInput.tsx
@frontend/src/components/chat/WelcomeInput.tsx
@frontend/src/components/chat/WorkspacePanel.tsx
@frontend/src/hooks/useChatState.ts
@frontend/src/lib/api.ts
@frontend/src/i18n/translations.ts

<interfaces>
<!-- Pattern source: existing toolbar buttons + WorkspacePanel apiFetch -->

From frontend/src/components/chat/InputActionBar.tsx (lines 32-37):
  <button className="flex h-8 w-8 items-center justify-center rounded-lg text-muted-foreground hover:text-foreground hover:bg-muted transition-colors">
    <Plus className="h-4 w-4" />
  </button>

From frontend/src/components/chat/InputActionBar.tsx (lines 53-69):
  conditional gating pattern with deepModeEnabled visibility flag

From frontend/src/lib/api.ts:
  apiFetch(path, init?) — supports custom signal via init.signal for AbortController

PUBLIC SETTINGS (W6 corrected):
  Existing endpoint /settings/public OR usePublicSettings() hook returns workspace_enabled.
  Per Plan 20-06's W6-corrected gating, the paperclip is gated on `workspace_enabled`
  (Phase 18 flag, ALREADY exposed via /settings/public). No backend extension needed
  for this plan — workspace_enabled is already publicly readable.
  Note: harness_enabled does NOT need to be exposed to the frontend since the upload
  endpoint no longer gates on it (W6 fix in Plan 20-06).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend useChatState with uploadingFiles slice + reducer cases</name>
  <files>frontend/src/hooks/useChatState.ts</files>
  <read_first>
    - frontend/src/hooks/useChatState.ts (recently extended in Plan 20-09; find the harnessRun slice as the insertion landmark)
    - .planning/phases/20-harness-engine-core-gatekeeper-post-harness-file-upload-lock/20-UI-SPEC.md lines 142-159 (FileUploadButton interaction contract; uploadingFiles requirement at L262/283)
  </read_first>
  <action>
    Add `uploadingFiles` slice next to `harnessRun` (Plan 20-09):

      type UploadingFile = {
        id: string                  // client-generated uuid
        filename: string
        sizeBytes: number
        percent: number             // 0..100
        abort: AbortController
        error?: string              // populated on failure
      }
      const [uploadingFiles, setUploadingFiles] = useState<Map<string, UploadingFile>>(new Map())

    Helper functions exposed in hook return:
      function startUpload(meta: { id: string; filename: string; sizeBytes: number; abort: AbortController }) {
        setUploadingFiles((prev) => {
          const m = new Map(prev)
          m.set(meta.id, { ...meta, percent: 0 })
          return m
        })
      }
      function updateUploadProgress(id: string, percent: number) {
        setUploadingFiles((prev) => {
          const m = new Map(prev)
          const entry = m.get(id)
          if (entry) m.set(id, { ...entry, percent })
          return m
        })
      }
      function completeUpload(id: string) {
        setUploadingFiles((prev) => {
          const m = new Map(prev)
          m.delete(id)
          return m
        })
      }
      function failUpload(id: string, error: string) {
        setUploadingFiles((prev) => {
          const m = new Map(prev)
          const entry = m.get(id)
          if (entry) m.set(id, { ...entry, error })
          return m
        })
      }

    Thread-switch reset: add `setUploadingFiles(new Map())` to the existing thread-switch useEffect — but ALSO call `abort.abort()` on each in-flight upload first to release pending fetches.

    Export `uploadingFiles, startUpload, updateUploadProgress, completeUpload, failUpload` from the hook return; add to ChatContext provider value.

    Verify TS:
      cd frontend &amp;&amp; npx tsc --noEmit
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit 2>&amp;1 | head -10 &amp;&amp; grep -c "uploadingFiles" src/hooks/useChatState.ts &amp;&amp; grep -c "startUpload\|completeUpload\|failUpload\|updateUploadProgress" src/hooks/useChatState.ts</automated>
  </verify>
  <done>
    uploadingFiles slice + 4 helper functions exported from useChatState; ChatContext exposes them.
  </done>
  <acceptance_criteria>
    - `cd frontend && grep -c "uploadingFiles" src/hooks/useChatState.ts` returns ≥3
    - `cd frontend && grep -c "startUpload\|updateUploadProgress\|completeUpload\|failUpload" src/hooks/useChatState.ts` returns ≥4
    - `cd frontend && grep -c "uploadingFiles" src/contexts/ChatContext.tsx` returns ≥1
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build FileUploadButton component + co-located test (W6 fix — gate on workspace_enabled per D-13)</name>
  <files>
    frontend/src/components/chat/FileUploadButton.tsx,
    frontend/src/components/chat/FileUploadButton.test.tsx
  </files>
  <read_first>
    - frontend/src/components/chat/InputActionBar.tsx FULL FILE (button geometry — lines 32-37, deepModeEnabled gating — lines 53-69)
    - frontend/src/components/chat/WorkspacePanel.tsx (apiFetch + workspace file types)
    - frontend/src/components/chat/CodeExecutionPanel.test.tsx (Vitest 3.2 + I18nProvider wrapper)
    - frontend/src/lib/api.ts (apiFetch signature + signal support)
    - frontend/src/hooks/usePublicSettings.ts (Phase 18 flag exposure — confirm workspace_enabled is exposed)
    - .planning/phases/20-harness-engine-core-gatekeeper-post-harness-file-upload-lock/20-UI-SPEC.md lines 144-158 (FileUploadButton visual + interaction contract)
    - .planning/phases/20-harness-engine-core-gatekeeper-post-harness-file-upload-lock/20-PATTERNS.md lines 793-833 (component adaptations)
    - .planning/phases/20-harness-engine-core-gatekeeper-post-harness-file-upload-lock/20-CONTEXT.md lines 81-83 (D-13 LOCKED — paperclip ALWAYS visible when WORKSPACE_ENABLED)
  </read_first>
  <action>
    Create `frontend/src/components/chat/FileUploadButton.tsx` (~150 LOC):

      import { useRef, useState } from 'react'
      import { Paperclip, Loader2, X } from 'lucide-react'
      import { useI18n } from '@/i18n/I18nContext'
      import { useChatContext } from '@/contexts/ChatContext'
      import { apiFetch } from '@/lib/api'
      import { usePublicSettings } from '@/hooks/usePublicSettings'  // existing hook
      import { toast } from '@/lib/toast'  // existing toast helper — confirm signature

      const MAX_BYTES = 25 * 1024 * 1024
      const ACCEPTED_MIME: Record<string, string> = {
        'application/pdf': 'pdf',
        'application/vnd.openxmlformats-officedocument.wordprocessingml.document': 'docx',
      }

      function uuid(): string {
        return (crypto.randomUUID &amp;&amp; crypto.randomUUID()) || `up-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`
      }

      export function FileUploadButton() {
        const { t } = useI18n()
        const { activeThreadId, uploadingFiles, startUpload, updateUploadProgress, completeUpload, failUpload } = useChatContext()
        const settings = usePublicSettings()
        const inputRef = useRef<HTMLInputElement>(null)
        const inFlight = uploadingFiles.size > 0

        // W6 fix (per CONTEXT.md D-13): paperclip is ALWAYS visible when WORKSPACE_ENABLED=true.
        // Works for both harness flows AND general workspace upload (when no harness is
        // registered, file lands as source='upload' with no gatekeeper trigger — see
        // rationale block at end of file).
        if (!settings?.workspace_enabled) return null

        async function onPick(e: React.ChangeEvent<HTMLInputElement>) {
          const file = e.target.files?.[0]
          if (file) await handleFile(file)
          if (inputRef.current) inputRef.current.value = ''
        }

        async function handleFile(file: File) {
          if (!activeThreadId) return
          // Client-side validation
          if (file.size > MAX_BYTES) {
            toast({
              role: 'alert',
              message: t('upload.tooLarge', { max: '25' }),
              duration: 5000,
            })
            return
          }
          if (!ACCEPTED_MIME[file.type]) {
            toast({
              role: 'alert',
              message: t('upload.wrongMime'),
              duration: 5000,
            })
            return
          }

          const id = uuid()
          const ctrl = new AbortController()
          startUpload({ id, filename: file.name, sizeBytes: file.size, abort: ctrl })

          const fd = new FormData()
          fd.append('file', file, file.name)

          try {
            await apiFetch(`/threads/${activeThreadId}/files/upload`, {
              method: 'POST',
              body: fd,
              signal: ctrl.signal,
            })
            completeUpload(id)
          } catch (err: any) {
            if (err?.name === 'AbortError') {
              completeUpload(id)
              toast({ message: t('upload.cancelled'), duration: 3000 })
              return
            }
            // Map backend error code → localized message
            const code = err?.body?.error || err?.detail?.error
            const localized =
              code === 'wrong_mime' || code === 'magic_byte_mismatch'
                ? t('upload.wrongMime')
                : code === 'upload_too_large'
                ? t('upload.tooLarge', { max: '25' })
                : t('upload.serverError')
            failUpload(id, localized)
            toast({ role: 'alert', message: localized, duration: 5000 })
          }
        }

        return (
          <>
            <button
              type="button"
              data-testid="file-upload-button"
              aria-label={t('chat.attachFile')}
              title={t('chat.attachFile.tooltip')}
              disabled={inFlight}
              onClick={() => inputRef.current?.click()}
              className={
                'flex h-8 w-8 items-center justify-center rounded-lg text-muted-foreground hover:text-foreground hover:bg-muted transition-colors ' +
                (inFlight ? 'opacity-50 cursor-not-allowed' : '')
              }
            >
              <Paperclip className="h-4 w-4" />
            </button>
            <input
              ref={inputRef}
              type="file"
              hidden
              accept=".docx,.pdf,application/pdf,application/vnd.openxmlformats-officedocument.wordprocessingml.document"
              onChange={onPick}
              data-testid="file-upload-input"
            />

            {/* In-flight progress card (one upload at a time per UI-SPEC L154) */}
            {Array.from(uploadingFiles.values()).map((u) => (
              <div
                key={u.id}
                role="status"
                aria-live="polite"
                data-testid="file-upload-progress"
                className="absolute bottom-full left-0 right-0 mx-4 mb-2 bg-card border border-border/50 rounded-lg px-3 py-2 flex items-center gap-2 text-xs"
              >
                <Loader2 className="h-3 w-3 animate-spin text-muted-foreground" aria-hidden />
                <span className="truncate flex-1">
                  {t('upload.inProgress', { filename: u.filename, percent: String(u.percent) })}
                </span>
                <button
                  type="button"
                  data-testid="file-upload-abort"
                  aria-label={t('common.cancel')}
                  onClick={() => u.abort.abort()}
                  className="h-5 w-5 flex items-center justify-center text-muted-foreground hover:text-foreground"
                >
                  <X className="h-3 w-3" />
                </button>
              </div>
            ))}
          </>
        )
      }

      // ---------------------------------------------------------------
      // Rationale (W6 fix): paperclip visibility decoupled from harness state.
      //
      // Per CONTEXT.md D-13, paperclip is "always visible when WORKSPACE_ENABLED=true".
      // This delivers two flows from a single button:
      //   (1) Harness flow: a registered harness's HarnessPrerequisites
      //       declares accepted_mime_types; the gatekeeper sees the new file in
      //       workspace and may emit the [TRIGGER_HARNESS] sentinel.
      //   (2) General workspace upload: when no harness is registered (or
      //       prerequisites aren't met), the file simply lands as source='upload'
      //       in workspace_files. The agent can reference it later via standard
      //       tools (search_workspace, read_workspace_file). No gatekeeper trigger.
      //
      // The button stays visible in both modes; the SUCCESS path differs only in
      // whether a gatekeeper SSE stream eventually fires. The MIME validation lives
      // server-side (Plan 20-06): the endpoint's accepted set covers PDF + DOCX
      // unconditionally — harness-specific MIME refinement is deferred (no current
      // harness narrows beyond PDF/DOCX).
      // ---------------------------------------------------------------

    NOTE: real upload progress (`updateUploadProgress`) requires a XHR-based progress event. fetch() does not expose upload progress. Acceptable v1.3 approach: keep the percent at 0 → show indeterminate Loader2 spinner with filename only. The percent string can show 0% throughout. Document this trade-off in the SUMMARY. If progress fidelity becomes a UAT requirement, swap apiFetch for a custom XMLHttpRequest in this component.

    No backend usePublicSettings extension needed — `workspace_enabled` was already exposed via /settings/public in Phase 18.

    Co-located test `frontend/src/components/chat/FileUploadButton.test.tsx` — 9 cases (W6: original 8 + 1 new for harness-disabled-but-workspace-enabled):
      (a) hidden when settings.workspace_enabled=false — assert button not rendered
      (b) visible when settings.workspace_enabled=true — data-testid="file-upload-button" present
      (c) rejects >25 MB locally — fire change event with a 26 MB file, assert toast t('upload.tooLarge'), assert apiFetch NOT called
      (d) rejects non-DOCX/PDF MIME locally — file with type 'application/zip', assert toast t('upload.wrongMime')
      (e) shows progress on multipart upload — successful apiFetch path, assert data-testid="file-upload-progress" appears then disappears on resolve
      (f) success removes progress UI — apiFetch resolves, progress card unmounts
      (g) abort during upload calls AbortController — click abort button, assert ctrl.abort called and progress UI removed
      (h) backend 4xx with code='wrong_mime' surfaces t('upload.wrongMime') — mock apiFetch to reject with err.body={error:'wrong_mime'}, assert toast text matches
      (i) **NEW (W6):** visible when workspace_enabled=true even if harness_enabled=false (or undefined) — set settings={workspace_enabled: true}; assert button rendered. This pins D-13's standalone-workspace use case.

    Mock @/lib/api, @/lib/toast, @/hooks/usePublicSettings; wrap renders with I18nProvider + mock ChatContext.

    Run:
      cd frontend &amp;&amp; npx vitest run src/components/chat/FileUploadButton.test.tsx
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/components/chat/FileUploadButton.test.tsx 2>&amp;1 | tee /tmp/fub.log; grep -E "Tests.*passed\|9 passed" /tmp/fub.log</automated>
  </verify>
  <done>
    FileUploadButton renders paperclip + hidden input + in-flight progress card; gated on workspace_enabled per D-13 (W6 fix); 9 tests pass.
  </done>
  <acceptance_criteria>
    - File `frontend/src/components/chat/FileUploadButton.tsx` exists
    - `cd frontend && grep -c "Paperclip" src/components/chat/FileUploadButton.tsx` returns ≥1
    - `cd frontend && grep -c "data-testid=\"file-upload-button\"" src/components/chat/FileUploadButton.tsx` returns 1
    - `cd frontend && grep -c "data-testid=\"file-upload-input\"" src/components/chat/FileUploadButton.tsx` returns 1
    - `cd frontend && grep -c "data-testid=\"file-upload-progress\"" src/components/chat/FileUploadButton.tsx` returns 1
    - `cd frontend && grep -c "data-testid=\"file-upload-abort\"" src/components/chat/FileUploadButton.tsx` returns 1
    - `cd frontend && grep -c "AbortController" src/components/chat/FileUploadButton.tsx` returns ≥1
    - `cd frontend && grep -c "MAX_BYTES\s*=\s*25\s*\*\s*1024\s*\*\s*1024" src/components/chat/FileUploadButton.tsx` returns 1
    - **W6 fix:** `cd frontend && grep -c "workspace_enabled" src/components/chat/FileUploadButton.tsx` returns ≥1 (gating per D-13)
    - **W6 fix:** `cd frontend && grep -c "settings?.harness_enabled\|settings\.harness_enabled" src/components/chat/FileUploadButton.tsx` returns 0 (NOT used as visibility gate)
    - `cd frontend && grep -c "files/upload" src/components/chat/FileUploadButton.tsx` returns ≥1
    - File `frontend/src/components/chat/FileUploadButton.test.tsx` exists
    - **W6 fix:** `cd frontend && grep -c "workspace_enabled.*true" src/components/chat/FileUploadButton.test.tsx` returns ≥1 (visibility test)
    - `cd frontend && npx vitest run src/components/chat/FileUploadButton.test.tsx` exits 0 with all cases passing
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Slot FileUploadButton into MessageInput.tsx + WelcomeInput.tsx (form-duplication rule)</name>
  <files>
    frontend/src/components/chat/MessageInput.tsx,
    frontend/src/components/chat/WelcomeInput.tsx
  </files>
  <read_first>
    - frontend/src/components/chat/MessageInput.tsx FULL FILE (find the InputActionBar host, line ~75)
    - frontend/src/components/chat/WelcomeInput.tsx FULL FILE (parallel input host)
    - .planning/phases/20-harness-engine-core-gatekeeper-post-harness-file-upload-lock/20-PATTERNS.md lines 901-918 (insertion guidance + form-duplication rule)
    - .planning/phases/20-harness-engine-core-gatekeeper-post-harness-file-upload-lock/20-UI-SPEC.md L144 (slot: "immediately to the right of the existing Plus button")

    PLANNER LOCKED DECISION (resolves PATTERNS.md fact-check #5): COMPOSE FileUploadButton alongside InputActionBar in BOTH MessageInput.tsx AND WelcomeInput.tsx. Do NOT extend InputActionBar's prop interface. Reasoning: keeps InputActionBar dumb and small; FileUploadButton is self-sufficient (reads its own context); UI-SPEC L144 says "inserted into InputActionBar" but the cleaner composition is to render it next to InputActionBar at the same flex level — visually identical, code simpler. Same hover/geometry classes, same toolbar position.
  </read_first>
  <action>
    Edit `frontend/src/components/chat/MessageInput.tsx`. Locate the existing InputActionBar invocation (around line 75) and add FileUploadButton as a sibling in the same flex container immediately before InputActionBar (so the paperclip appears as the FIRST toolbar icon — UI-SPEC L144 says "to the right of the existing Plus button" but the Plus button is itself inside InputActionBar; pragmatic compromise: render FileUploadButton just before InputActionBar so the toolbar reads "[paperclip][Plus][FileText][...InputActionBar items]"). Visually consistent with shadcn pill-toolbar pattern.

    1. Add import:
       import { FileUploadButton } from './FileUploadButton'

    2. Wrap or extend the toolbar wrapper to include FileUploadButton:
       <div className="flex items-center gap-1 relative">    {/* relative needed for absolute progress card */}
         <FileUploadButton />
         <InputActionBar
           onSend={handleSend}
           ...
         />
       </div>

    Repeat the same edit in `frontend/src/components/chat/WelcomeInput.tsx`. The WelcomeInput's toolbar layout differs slightly; place FileUploadButton in the equivalent toolbar container (look for any existing icon button — that is the right location).

    Verify TS + lint + a quick visual smoke (planner: open dev server briefly if available — optional, deferred to UAT).

    Run:
      cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit 2>&amp;1 | head -10 &amp;&amp; grep -c "<FileUploadButton" src/components/chat/MessageInput.tsx &amp;&amp; grep -c "<FileUploadButton" src/components/chat/WelcomeInput.tsx</automated>
  </verify>
  <done>
    Both input hosts import and render FileUploadButton; tsc + lint pass.
  </done>
  <acceptance_criteria>
    - `cd frontend && grep -c "import.*FileUploadButton" src/components/chat/MessageInput.tsx` returns 1
    - `cd frontend && grep -c "<FileUploadButton" src/components/chat/MessageInput.tsx` returns 1
    - `cd frontend && grep -c "import.*FileUploadButton" src/components/chat/WelcomeInput.tsx` returns 1
    - `cd frontend && grep -c "<FileUploadButton" src/components/chat/WelcomeInput.tsx` returns 1
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npm run lint` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /files/upload | untrusted file body — server validates magic bytes (Plan 20-06) |
| client → AbortController | local; no security boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-10-01 | T (Tampering) | client-side validation bypass | mitigate | Server-side validation in Plan 20-06 (size + MIME + magic bytes) is the authoritative check. Client-side is UX-only. |
| T-20-10-02 | I (Information disclosure) | filename leak in toast | accept | Toast shows filename for UX; user uploaded the file knowingly. |
| T-20-10-03 | D (Denial of service) | rapid-click upload spam | mitigate | inFlight=true disables button while upload in progress; sequential queue per UI-SPEC. |
| T-20-10-04 | I (Information disclosure) | error.body surfaces backend internals | mitigate | Only `code` mapped to localized strings — no raw detail/path/exception surfaced to user. |
</threat_model>

<verification>
- All 9 component tests pass.
- tsc + lint clean.
- FileUploadButton renders in MessageInput AND WelcomeInput.
- workspace_enabled gate (W6 — per D-13) hides button cleanly when off; visibility decoupled from harness_enabled.
- AbortController flow tested.
</verification>

<success_criteria>
1. With workspace_enabled=true, paperclip appears in chat input toolbar (regardless of harness_enabled).
2. Selecting a valid DOCX/PDF triggers POST /files/upload; success removes progress UI.
3. Selecting >25 MB shows toast "File too large — max 25 MB" with no network call.
4. Aborting in-flight calls AbortController; backend cancellation handled by fetch's signal.
5. With workspace_enabled=false, paperclip is hidden.
6. **W6:** When workspace_enabled=true and harness_enabled=false, paperclip remains visible; uploaded file lands as source='upload' with no gatekeeper trigger (D-13 standalone-workspace case).
</success_criteria>

<output>
After completion, create `.planning/phases/20-harness-engine-core-gatekeeper-post-harness-file-upload-lock/20-10-SUMMARY.md` documenting: FileUploadButton component shape, uploadingFiles slice, MessageInput + WelcomeInput slot locations, W6-corrected workspace_enabled gating per D-13, locked decision on InputActionBar composition, 9 new tests.
</output>
