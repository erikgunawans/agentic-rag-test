---
phase: 19
plan: 09
type: execute
wave: 5
depends_on: [19-04, 19-05, 19-06, 19-08]
files_modified:
  - backend/tests/integration/test_phase19_e2e.py
autonomous: true
requirements: [TASK-01, TASK-02, TASK-03, TASK-04, TASK-05, TASK-06, TASK-07, ASK-01, ASK-02, ASK-03, ASK-04, STATUS-01, STATUS-02, STATUS-03, STATUS-04, STATUS-05, STATUS-06]
security:
  asvs_level: 1
  block_on: high
must_haves:
  truths:
    - "End-to-end test exercises task tool happy path through real chat.py SSE → sub_agent_loop → workspace tools → final assistant text [satisfies TASK-01..04, TASK-07]"
    - "context_files pre-load test verifies the actual <context_file> XML wrapping in the LLM-bound payload [satisfies TASK-03, D-08]"
    - "Sub-agent failure isolation test confirms parent loop continues after sub-agent crash [satisfies TASK-05, STATUS-04]"
    - "ask_user pause/resume two-request flow drives /chat through the real router [satisfies ASK-01..04]"
    - "Two-user RLS test asserts User A cannot read User B's agent_runs / messages with parent_task_id [satisfies SEC-01 / threat T-19-03]"
    - "Sub-agent inherits parent JWT — workspace shared via RLS scope [satisfies SEC-02 / threat T-19-22]"
    - "Status indicator transitions (working → waiting_for_user → working → complete) verified [satisfies STATUS-01]"
    - "Append-only error roundtrip: tool fails → LLM sees structured error in next round → recovers without retry [satisfies STATUS-02, STATUS-03]"
    - "STATUS-06 resume: paused thread + follow-up message → agent reads existing todos/workspace and continues [satisfies STATUS-06]"
    - "Privacy invariant test: parent writes file with PII → call task with context_files → assert PII-bound LLM payload anonymized via parent registry [satisfies SEC-04 / threat T-19-21]"
    - "Byte-identical fallback: SUB_AGENT_ENABLED=False → POSITIVE assertion that SSE event-type set is exactly the legacy set {delta, done, tool_start, tool_result} — no task_*, no agent_status [satisfies D-17 / D-30]"
    - "TASK-06 coexistence: existing v1.0 multi-agent classifier (analyze_document, explore_knowledge_base) still works [satisfies TASK-06]"
  artifacts:
    - path: "backend/tests/integration/test_phase19_e2e.py"
      provides: "End-to-end Phase 19 test suite — 12 cases covering all 17 REQ-IDs cumulatively"
      contains: "test_task_happy_path_e2e"
  key_links:
    - from: "backend/tests/integration/test_phase19_e2e.py"
      to: "backend/app/routers/chat.py stream_chat"
      via: "TestClient drives /chat POST and captures SSE"
      pattern: "POST .*/chat"
---

<objective>
End-to-end pytest suite exercising the entire Phase 19 backend through real `/chat` POSTs, real Supabase, and stub LLMs. This plan is the verification gate — 17/17 requirements must be observable via these tests before Phase 19 is marked done.

Purpose: Plans 19-01 through 19-08 each tested narrow slices. This plan stitches them together: task tool dispatched in one round, ask_user pause-and-resume across two requests, RLS isolation across two users, privacy invariant on context_files, byte-identical fallback (positive event-set assertion). Without this plan, the phase has narrow per-plan tests but no cross-cutting verification.

Output: One large pytest file (~600 lines) with 12 cases — cumulative coverage of TASK-01..07, ASK-01..04, STATUS-01..06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-sub-agent-delegation-ask-user-status-recovery/19-CONTEXT.md
@.planning/phases/19-sub-agent-delegation-ask-user-status-recovery/19-PATTERNS.md
@.planning/phases/19-sub-agent-delegation-ask-user-status-recovery/19-04-SUMMARY.md
@.planning/phases/19-sub-agent-delegation-ask-user-status-recovery/19-05-SUMMARY.md
@.planning/phases/19-sub-agent-delegation-ask-user-status-recovery/19-06-SUMMARY.md
@.planning/phases/19-sub-agent-delegation-ask-user-status-recovery/19-08-SUMMARY.md

@backend/app/routers/chat.py
@backend/app/services/sub_agent_loop.py
@backend/app/services/agent_runs_service.py
@backend/tests/integration/test_deep_mode_chat_loop.py
@backend/tests/integration/test_chat_resume_flow.py
@backend/tests/integration/test_sub_agent_loop.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write Phase 19 E2E test suite (12 cases)</name>
  <files>backend/tests/integration/test_phase19_e2e.py</files>
  <read_first>
    - backend/tests/integration/test_deep_mode_chat_loop.py (full — chat-loop SSE harness pattern)
    - backend/tests/integration/test_chat_resume_flow.py (from 19-05; resume flow harness)
    - backend/tests/integration/test_sub_agent_loop.py (from 19-03; stub LLM driver pattern)
    - .planning/phases/19-sub-agent-delegation-ask-user-status-recovery/19-CONTEXT.md D-32 (full test scope spec)
    - .planning/phases/19-sub-agent-delegation-ask-user-status-recovery/19-PATTERNS.md L1044-1059 (sub-agent loop test scope)
    - CLAUDE.md (test account credentials, env vars)
  </read_first>
  <behavior>
    - Test 1: `test_task_happy_path_e2e` (TASK-01..04, TASK-07): POST /chat deep_mode=true; stub LLM round 1 returns `task(description="say hi")`; sub-agent stub returns text "hi"; stub LLM round 2 (post-task) returns final assistant "Done". SSE stream contains: task_start, task_complete{result:"hi"}, delta with "Done", agent_status complete, done. messages table has 2 messages (one assistant with task tool_call + tool_result, one final assistant) AND a sub-agent message with parent_task_id set.
    - Test 2: `test_task_context_files_preload_e2e` (TASK-03 / D-08): create workspace file `notes.md` with content "ALPHA"; stub LLM calls `task(description="read notes", context_files=["notes.md"])`; sub-agent stub captures its first user message; assert it contains `<context_file path="notes.md">` AND "ALPHA".
    - Test 3: `test_sub_agent_failure_isolation_e2e` (TASK-05, STATUS-04): sub-agent stub raises RuntimeError("fail"); SSE stream contains `task_error{error:"sub_agent_failed", code:"TASK_LOOP_CRASH"}`; parent's tool_result row stores the structured error JSON; parent loop continues to next round (LLM stub round 2 returns text "I'll try a different approach"); final assistant message persists.
    - Test 4: `test_ask_user_pause_and_resume_e2e` (ASK-01..04): two-request sequence — POST 1: stub LLM returns `ask_user(question="confirm?")`; SSE: agent_status{waiting_for_user}, ask_user{question}, done; agent_runs row status='waiting_for_user'. POST 2: body.message="yes"; stub LLM (now seeing tool_result with content "yes") returns final "Confirmed."; SSE: agent_status{working}, delta "Confirmed.", agent_status{complete}, done. agent_runs row transitioned to 'complete'.
    - Test 5: `test_rls_isolation_two_users_agent_runs` (SEC-01 / threat T-19-03): User A creates a thread, runs deep mode (stub creates agent_runs row); User B (different JWT) attempts `SELECT FROM agent_runs WHERE thread_id=A's_thread_id` — returns 0 rows. ALSO: User B's authed client cannot SELECT messages with parent_task_id from User A's thread.
    - Test 6: `test_sub_agent_inherits_parent_jwt` (SEC-02 / threat T-19-22): sub-agent calls workspace_service.read_file(thread_id=parent_thread, path="notes.md", token=parent_token); reads succeed because RLS scope inherited; assert no service-role bypass attempted.
    - Test 7: `test_status_indicator_transitions_e2e` (STATUS-01): collect all `agent_status` events from a happy-path session AND from a paused-then-resumed session. Assert sequences:
      - Happy: ['working', 'complete']
      - Pause/resume: ['working', 'waiting_for_user', 'working', 'complete']
    - Test 8: `test_append_only_error_roundtrip_e2e` (STATUS-02, STATUS-03): tool stub returns structured error `{"error":"invalid_path","code":"WS_INVALID_PATH","detail":"..."}`. Stub LLM round 2 (now seeing the error result in conversation context) returns assistant text "Let me try a different path: foo.md" without calling any retry helper. The persisted tool_result JSONB carries the exact structured error shape. NO automatic retry happened (tool called once).
    - Test 9: `test_status_06_resume_after_pause_with_existing_state_e2e` (STATUS-06): create a thread, run deep mode that writes 3 todos via write_todos and 2 workspace files; pause via ask_user. POST 2: agent reads existing todos via read_todos and existing workspace via list_files (verify both LLM tool_calls in SSE stream); continues without re-priming. agent_todos rows persist; workspace_files rows persist.
    - Test 10: `test_privacy_invariant_context_files_e2e` (SEC-04 / threat T-19-21): parent writes a workspace file containing PII (e.g., "John Doe SSN 123-45-6789"); stub LLM calls `task(description="...", context_files=["pii.md"])`; capture the actual JSON payload sent to OpenRouter (mock the SDK call); assert the captured payload's first user message has the PII anonymized to surrogate (e.g., "<PERSON_1>" / "<NUMBER_1>") via the parent's ConversationRegistry. Mirrors Phase 18 D-15 invariant test pattern.
    - **Test 11: `test_sub_agent_disabled_byte_identical_fallback_e2e` (D-17 / D-30) — POSITIVE event-set assertion** (revision-1 Warning 9 fix): set `SUB_AGENT_ENABLED=False` (monkeypatch); POST /chat deep_mode=true with body.message="hello"; stub LLM returns one tool_call (e.g., `search_documents`) followed by final assistant text. Capture the FULL ordered SSE event-type sequence from the stream as a list (e.g., `['delta', 'tool_start', 'tool_result', 'delta', 'done']`). Assertions:
      1. **POSITIVE event-set assertion (NEW):** the SET of distinct event types in the captured stream MUST be a subset of the legacy event set: `{'delta', 'done', 'tool_start', 'tool_result'}`. Use Python: `assert set(captured_event_types) <= {'delta', 'done', 'tool_start', 'tool_result'}, f"Unexpected event types: {set(captured_event_types) - {'delta', 'done', 'tool_start', 'tool_result'}}"`.
      2. **Negative-space assertions (existing):** NO `task_start`, NO `task_complete`, NO `task_error`, NO `agent_status` events appear anywhere in the stream. Confirmed by the positive set assertion above (their absence is implied by the subset relation).
      3. **Database-level invariants:** NO agent_runs rows created (`SELECT count(*) FROM agent_runs WHERE thread_id=<test_thread>` returns 0); NO messages rows with non-NULL `parent_task_id` (`SELECT count(*) FROM messages WHERE parent_task_id IS NOT NULL` returns 0).
      4. **Resume-detection short-circuit:** even if a stale `waiting_for_user` agent_runs row exists in the database (insert one via service-role fixture), the resume-detection branch in `stream_chat` MUST short-circuit when `sub_agent_enabled=False` and treat the request as a normal new message.

      This makes the byte-identical-fallback invariant a POSITIVE assertion (the captured event set IS exactly the legacy set), not just a negative-space check. If 19-06's Site A/C/D yields are not properly gated (Blocker 4 regression), this test FAILS because `agent_status` would appear in the captured set.
    - Test 12: `test_task06_coexistence_with_v1_0_multi_agent_classifier` (TASK-06): in standard mode (deep_mode=false), POST /chat with a doc-analysis-shaped query; assert the existing `agent_service.classify_intent` is invoked and returns a sub-agent (research / data_analyst / general / explorer); the new `task` tool is NOT registered for non-deep-mode requests; existing `analyze_document` and `explore_knowledge_base` SSE event names appear unchanged.
  </behavior>
  <action>
    Create `backend/tests/integration/test_phase19_e2e.py` (~600 lines):
    - Module-level fixtures: `_login`, `_create_thread_svc`, `_delete_thread_svc`, `_capture_sse_stream(response) -> list[dict]`, `_capture_sse_event_types(response) -> list[str]` (returns ordered list of `event['type']` values), `_stub_llm(rounds: list[dict])`, `_capture_llm_payloads()` (records every JSON sent to OpenRouter SDK).
    - Use `httpx.AsyncClient` against the FastAPI TestClient or live local server.
    - Real Supabase test project (test@test.com / test-2@test.com fixtures).
    - All tests use `pytest.mark.asyncio` and per-test thread fixtures with teardown.
    - For LLM stubbing, patch `client.chat.completions.create` (or the OpenRouter wrapper) to return scripted responses keyed by round index.
    - For sub-agent stubbing, patch `run_sub_agent_loop` or its inner LLM call to return scripted events.
    - Tests skip with informative message if env vars `TEST_EMAIL`, `TEST_PASSWORD`, `TEST_EMAIL_2`, `TEST_PASSWORD_2`, `OPENROUTER_API_KEY` (or stub equivalent) are missing.
    - For Test 11 specifically: use `_capture_sse_event_types` to build the captured list, then run BOTH the positive subset assertion AND the negative-space assertions. The positive assertion is the load-bearing one; the negatives are informative on failure.

    All 12 tests target real router code; expected outcome: ALL PASS after 19-04..19-08 are merged. Run pytest: `12 passed`.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; source venv/bin/activate &amp;&amp; TEST_EMAIL="test@test.com" TEST_PASSWORD='!*-3-3?3uZ?b$v&amp;' TEST_EMAIL_2="test-2@test.com" TEST_PASSWORD_2='fK4$Wd?HGKmb#A2' SUB_AGENT_ENABLED=true DEEP_MODE_ENABLED=true pytest tests/integration/test_phase19_e2e.py -v 2>&amp;1 | tail -3 | grep -E "12 passed"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `backend/tests/integration/test_phase19_e2e.py`
    - `grep -c "^async def test_\\|^def test_" backend/tests/integration/test_phase19_e2e.py` returns 12
    - `grep -c "test_privacy_invariant_context_files_e2e" backend/tests/integration/test_phase19_e2e.py` returns 1 (privacy invariant present — required for SEC-04)
    - `grep -c "test_rls_isolation_two_users_agent_runs" backend/tests/integration/test_phase19_e2e.py` returns 1 (T-19-03 mitigation present)
    - `grep -c "test_sub_agent_disabled_byte_identical_fallback_e2e" backend/tests/integration/test_phase19_e2e.py` returns 1 (byte-identical fallback)
    - **Positive event-set assertion present in Test 11 (revision-1 Warning 9 fix):** `grep -E "set\(captured_event_types\) <= \{'delta', 'done', 'tool_start', 'tool_result'\}" backend/tests/integration/test_phase19_e2e.py` matches at least 1 location (or any equivalent subset-of-legacy-set assertion using Python set semantics)
    - `grep -c "test_task06_coexistence_with_v1_0_multi_agent_classifier" backend/tests/integration/test_phase19_e2e.py` returns 1 (TASK-06 coexistence)
    - `pytest backend/tests/integration/test_phase19_e2e.py -v` reports `12 passed`
  </acceptance_criteria>
  <done>E2E suite committed; all 12 tests pass against the integrated 19-04..19-08 backend; Test 11 includes positive byte-identical-stream assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture → /chat POST | Real HTTP boundary, real Supabase, stubbed LLM only |
| Two test users | Distinct JWTs verify cross-user isolation (T-19-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Information disclosure | RLS isolation gap (User A vs User B) | mitigate | Test 5 explicitly drives both users against agent_runs and parent_task_id-tagged messages; asserts cross-user reads return 0 rows. |
| T-19-21 | Information disclosure | PII leakage to cloud LLM via context_files | mitigate | Test 10 captures the actual JSON payload to OpenRouter and asserts PII has been anonymized (mirrors Phase 18 D-15). |
| T-19-22 | Elevation of privilege | Sub-agent JWT scope abuse | mitigate | Test 6 verifies sub-agent uses parent_token only; no service-role escalation. |
| T-19-FALLBACK | Tampering | Phase 19 features leak into byte-identical fallback path | mitigate | Test 11 captures the FULL ordered SSE event-type sequence and asserts the set is a subset of `{delta, done, tool_start, tool_result}` (positive assertion); also asserts zero agent_runs rows, zero parent_task_id messages, and resume-detection short-circuit. |
| T-19-COEXIST | Behavior deviation | TASK-06 multi-agent classifier broken | mitigate | Test 12 verifies analyze_document / explore_knowledge_base path unchanged. |
</threat_model>

<verification>
1. All 12 E2E tests pass against real Supabase + stubbed LLM.
2. RLS isolation explicitly tested with two real users.
3. Privacy invariant explicitly tested by capturing the LLM-bound JSON payload.
4. JWT inheritance verified.
5. Byte-identical fallback verified via POSITIVE event-set subset assertion (revision-1 strengthens the negative-space check).
6. TASK-06 coexistence verified.
7. Append-only error contract roundtrip verified (LLM recovers without retry helper).
</verification>

<success_criteria>
- [ ] All 12 E2E tests pass
- [ ] All 17 Phase 19 REQ-IDs covered
- [ ] All HIGH-severity threats explicitly tested (T-19-03, T-19-21, T-19-22)
- [ ] Byte-identical fallback verified by positive event-set assertion
- [ ] TASK-06 coexistence verified
</success_criteria>

<output>
After completion, create `.planning/phases/19-sub-agent-delegation-ask-user-status-recovery/19-09-SUMMARY.md` capturing:
- 12/12 test results
- Threat coverage map (which test addresses which threat)
- REQ-ID-to-test mapping (all 17 covered)
- Test 11 positive event-set assertion outcome (which event types appeared in the SUB_AGENT_ENABLED=False stream)
- Notable findings during integration (if any)
</output>
