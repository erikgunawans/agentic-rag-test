---
phase: 19
plan: 07
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - frontend/src/hooks/useChatState.ts
  - frontend/src/contexts/ChatContext.tsx
  - frontend/src/components/chat/AgentStatusChip.tsx
  - frontend/src/components/chat/TaskPanel.tsx
  - frontend/src/components/chat/MessageView.tsx
  - frontend/src/lib/database.types.ts
  - frontend/src/i18n/translations.ts
  - frontend/src/pages/ChatPage.tsx
autonomous: true
requirements: [TASK-07, ASK-02, STATUS-01]
security:
  asvs_level: 1
  block_on: high
must_haves:
  truths:
    - "useChatState exposes agentStatus + tasks slices with reducer cases for SSE events [satisfies STATUS-01 / D-24]"
    - "AgentStatusChip renders 4 visual states (working/waiting_for_user/complete/error) with auto-fade on complete [satisfies STATUS-01 / D-26]"
    - "TaskPanel renders one card per task_id with nested tool calls + status icon [satisfies TASK-07 / D-25]"
    - "MessageView renders question-bubble variant when ask_user tool_call has no matching tool_result [satisfies ASK-02 / D-27, D-14]"
    - "All Phase 19 i18n strings present in BOTH id and en locales [satisfies UI-SPEC contract]"
    - "AgentStatusChip rendered in chat-route component (NOT AppLayout.tsx) — sticky top-0 z-10 [satisfies UI-SPEC L455]"
    - "TaskPanel rendered in chat-route component to right of WorkspacePanel [satisfies UI-SPEC L457]"
    - "Glass rule honored: bg-background, NO backdrop-blur on TaskPanel [satisfies CLAUDE.md persistent-panel rule]"
    - "SubAgentPanel.tsx is NOT modified — TaskPanel is a new independent component [satisfies UI-SPEC L459]"
  artifacts:
    - path: "frontend/src/components/chat/AgentStatusChip.tsx"
      provides: "Header chip showing agent run status"
      exports: ["AgentStatusChip"]
    - path: "frontend/src/components/chat/TaskPanel.tsx"
      provides: "Sub-agent task panel rendering one card per task_id"
      exports: ["TaskPanel"]
    - path: "frontend/src/hooks/useChatState.ts"
      provides: "agentStatus + tasks slices + SSE reducer cases"
      contains: "AgentStatus"
    - path: "frontend/src/i18n/translations.ts"
      provides: "Phase 19 i18n keys (12 keys × 2 locales = 24 entries)"
      contains: "agentStatus.working"
  key_links:
    - from: "frontend/src/components/chat/AgentStatusChip.tsx"
      to: "frontend/src/contexts/ChatContext"
      via: "agentStatus slice consumed via useChatContext"
      pattern: "useChatContext"
    - from: "frontend/src/components/chat/TaskPanel.tsx"
      to: "frontend/src/contexts/ChatContext"
      via: "tasks Map slice consumed via useChatContext"
      pattern: "useChatContext"
    - from: "frontend/src/hooks/useChatState.ts"
      to: "frontend/src/lib/database.types.SSEEvent"
      via: "SSEEvent union extended with agent_status / task_* / ask_user variants"
      pattern: "agent_status|task_start|task_complete|task_error"
---

<objective>
Build the entire Phase 19 frontend in one plan (4 tasks per revision-1 split): state slices, two new components (AgentStatusChip, TaskPanel), MessageView question-bubble variant, i18n strings (12 keys × 2 locales), SSEEvent type extensions, and ChatPage layout integration.

Purpose: This is the user-visible payoff for the entire phase. Without it, the backend SSE events go nowhere. Independent from 19-04..19-06 because the SSE event shapes are locked in CONTEXT.md/UI-SPEC.md and PATTERNS.md — frontend can implement against the contract regardless of when backend ships.

Wave 2 placement: Frontend gates only on 19-01 (migration applied) so the backend can be exercised end-to-end. Frontend tests use mocked SSE events; live integration with 19-04..19-06 happens in 19-09 / 19-10.

Output: 8 frontend file modifications/additions implementing the full UI-SPEC contract. Tests (Vitest) ship in 19-10.

Revision-1 Warning 7 fix: Original Task 2 covered 5 files (borderline-large). Per revision-1 it is split into Task 2a (AgentStatusChip + i18n strings for chip + ChatPage layout integration) and Task 2b (TaskPanel + MessageView question-bubble + remaining i18n + useChatState slice extensions). Each task ≤ 3 files. Both tasks live in this same plan file — no new plan file created.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-sub-agent-delegation-ask-user-status-recovery/19-CONTEXT.md
@.planning/phases/19-sub-agent-delegation-ask-user-status-recovery/19-PATTERNS.md
@.planning/phases/19-sub-agent-delegation-ask-user-status-recovery/19-UI-SPEC.md

@frontend/src/hooks/useChatState.ts
@frontend/src/contexts/ChatContext.tsx
@frontend/src/components/chat/PlanPanel.tsx
@frontend/src/components/chat/WorkspacePanel.tsx
@frontend/src/components/chat/SubAgentPanel.tsx
@frontend/src/components/chat/MessageView.tsx
@frontend/src/components/chat/AgentBadge.tsx
@frontend/src/lib/database.types.ts
@frontend/src/i18n/translations.ts
@frontend/src/pages/ChatPage.tsx

<interfaces>
<!-- Existing — DO NOT redefine. -->

```ts
// from contexts/ChatContext
useChatContext(): {
  // existing slices: messages, todos, workspaceFiles, ...
  // NEW (this plan adds):
  agentStatus: AgentStatus
  setAgentStatus: (s: AgentStatus) => void
  tasks: Map<string, TaskState>
}

// from i18n/I18nContext
useI18n(): { t: (key: string) => string; locale: 'id' | 'en' }
```

<!-- New types this plan creates: -->

```ts
export type AgentStatus = 'working' | 'waiting_for_user' | 'complete' | 'error' | null

export type TaskToolCall = {
  toolCallId: string
  tool: string
  input?: Record<string, unknown>
  output?: Record<string, unknown> | string
}

export type TaskState = {
  taskId: string
  description: string
  contextFiles: string[]
  toolCalls: TaskToolCall[]
  status: 'running' | 'complete' | 'error'
  result?: string
  error?: { error: string; code: string; detail?: string }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend SSEEvent types + useChatState slices + ChatContext</name>
  <files>
    frontend/src/lib/database.types.ts,
    frontend/src/hooks/useChatState.ts,
    frontend/src/contexts/ChatContext.tsx
  </files>
  <read_first>
    - frontend/src/hooks/useChatState.ts (full — primary self-extension target; pay attention to L60 todos slice, L70 workspaceFiles slice, L96-126 reset effects, L223-231 send-start reset, L287-295 tool_start/tool_result paths, L337-374 SSE handler, L437-466 return tuple)
    - frontend/src/contexts/ChatContext.tsx (full — return type that consumes useChatState)
    - frontend/src/lib/database.types.ts (find the SSEEvent union)
    - .planning/phases/19-sub-agent-delegation-ask-user-status-recovery/19-PATTERNS.md L854-949 (verbatim TypeScript excerpts: type defs, slice declarations, SSE handler additions, reset hooks, return-tuple addition)
    - .planning/phases/19-sub-agent-delegation-ask-user-status-recovery/19-UI-SPEC.md L181 (Map keyed by task_id)
  </read_first>
  <behavior>
    - `frontend && npx tsc --noEmit` passes after changes.
    - useChatState exposes `agentStatus`, `setAgentStatus`, `tasks` (Map<string, TaskState>).
    - SSE event handlers correctly mutate the new slices.
    - Polymorphic tool_start/tool_result fall-through preserved (when no task_id, route to active tools as before).
    - Slices reset on thread switch and on send.
  </behavior>
  <action>
    **Part A — `frontend/src/lib/database.types.ts`**:

    Find the `SSEEvent` union (or equivalent). Extend with:
    ```ts
    | { type: 'agent_status'; status: 'working' | 'waiting_for_user' | 'complete' | 'error'; detail?: string }
    | { type: 'task_start'; task_id: string; description: string; context_files?: string[] }
    | { type: 'task_complete'; task_id: string; result: string }
    | { type: 'task_error'; task_id: string; error: string; code: string; detail?: string }
    | { type: 'ask_user'; question: string }
    ```
    Also add OPTIONAL `task_id?: string` to existing `tool_start` and `tool_result` event variants (D-06 — nested tagging).

    **Part B — `frontend/src/hooks/useChatState.ts`**:

    Add type definitions (verbatim from PATTERNS.md L856-873):
    ```ts
    export type AgentStatus = 'working' | 'waiting_for_user' | 'complete' | 'error' | null
    export type TaskToolCall = { toolCallId: string; tool: string; input?: Record<string, unknown>; output?: Record<string, unknown> | string }
    export type TaskState = { taskId: string; description: string; contextFiles: string[]; toolCalls: TaskToolCall[]; status: 'running' | 'complete' | 'error'; result?: string; error?: { error: string; code: string; detail?: string } }
    ```

    Add slice declarations near existing `todos`, `workspaceFiles` slices (PATTERNS.md L878-881):
    ```ts
    const [agentStatus, setAgentStatus] = useState<AgentStatus>(null)
    const [tasks, setTasks] = useState<Map<string, TaskState>>(new Map())
    ```

    Add SSE handler cases (PATTERNS.md L885-921). For `tool_start` and `tool_result`, ALSO add a polymorphic check: when `'task_id' in event && event.task_id`, route to tasks slice; otherwise fall through to existing `setActiveTools` / `setToolResults` paths.

    Add reset effects (PATTERNS.md L926-929):
    ```ts
    useEffect(() => { setAgentStatus(null); setTasks(new Map()) }, [activeThreadId])
    ```

    Add reset on send (alongside existing setActiveTools([]) etc. ~L223-231):
    ```ts
    setAgentStatus(null)
    setTasks(new Map())
    ```

    Add return-tuple fields (PATTERNS.md L941-943):
    ```ts
    agentStatus, setAgentStatus, tasks,
    ```

    **Part C — `frontend/src/contexts/ChatContext.tsx`**:

    Extend `ChatContextValue` type (or equivalent) to include:
    ```ts
    agentStatus: AgentStatus
    setAgentStatus: (s: AgentStatus) => void
    tasks: Map<string, TaskState>
    ```
    Pass through from `useChatState()` to the provider value.

    Run `cd frontend && npx tsc --noEmit` — must pass with zero errors.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AgentStatus" frontend/src/hooks/useChatState.ts` returns at least 3 (type def + slice + return)
    - `grep -c "TaskState" frontend/src/hooks/useChatState.ts` returns at least 3
    - **Map type literal grep — literal `<` and `>`, NOT HTML entities** (revision-1 Warning 8 fix): `grep -c "Map<string, TaskState>" frontend/src/hooks/useChatState.ts` returns at least 1
    - `grep -E "type: 'agent_status'" frontend/src/lib/database.types.ts` matches
    - `grep -E "type: 'task_start'" frontend/src/lib/database.types.ts` matches
    - `grep -E "agentStatus" frontend/src/contexts/ChatContext.tsx` matches at least 1
    - `cd frontend && npx tsc --noEmit` exits 0 (zero TS errors)
  </acceptance_criteria>
  <done>State slices wired; SSE events mutate slices correctly; types pass tsc.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2a: Build AgentStatusChip + chip i18n strings + ChatPage AgentStatusChip integration</name>
  <files>
    frontend/src/components/chat/AgentStatusChip.tsx,
    frontend/src/i18n/translations.ts,
    frontend/src/pages/ChatPage.tsx
  </files>
  <read_first>
    - .planning/phases/19-sub-agent-delegation-ask-user-status-recovery/19-UI-SPEC.md (full file — visual contract; verbatim copy / colors / spacing / accessibility)
    - .planning/phases/19-sub-agent-delegation-ask-user-status-recovery/19-PATTERNS.md L605-700 (verbatim TSX excerpts for AgentStatusChip)
    - .planning/phases/19-sub-agent-delegation-ask-user-status-recovery/19-PATTERNS.md L953-995 (i18n insertions verbatim)
    - frontend/src/components/chat/AgentBadge.tsx (chip styling precedent)
    - frontend/src/pages/ChatPage.tsx (or equivalent route component for layout integration)
    - CLAUDE.md (glass rule)
  </read_first>
  <behavior>
    - AgentStatusChip renders 4 visual states + auto-fade on complete (3000ms) + remains in DOM as sr-only when null.
    - All AgentStatusChip-related i18n keys present in BOTH locales (4 status labels + chip aria-label keys).
    - AgentStatusChip rendered in ChatPage (NOT AppLayout) as `sticky top-0 z-10`.
    - `cd frontend && npx tsc --noEmit && npm run lint` passes (no new errors from AgentStatusChip / ChatPage edits).
  </behavior>
  <action>
    **Part A — `frontend/src/components/chat/AgentStatusChip.tsx`** (NEW):

    Copy verbatim from PATTERNS.md L612-678 (imports, ChipIcon switch, full component with auto-fade useEffect + null-state sr-only container). Use lucide-react icons: `MessageCircleQuestion`, `CheckCircle2`, `AlertCircle`. Apply UI-SPEC color overrides exactly:
    - working: `bg-zinc-100 dark:bg-zinc-800 text-zinc-700 dark:text-zinc-300` + pulsing primary dot
    - waiting_for_user: `bg-purple-50 dark:bg-purple-950/40 text-purple-700 dark:text-purple-300` + MessageCircleQuestion
    - complete: `bg-green-50 dark:bg-green-950/40 text-green-700 dark:text-green-300` + CheckCircle2
    - error: `bg-red-50 dark:bg-red-950/40 text-red-700 dark:text-red-300` + AlertCircle

    Chip anatomy: `inline-flex items-center gap-2 px-2 py-1 rounded-full border border-current/20 transition-all duration-200`.
    Auto-fade: `useEffect` with `setTimeout(3000)` setting agentStatus to null on complete; cleanup via `clearTimeout`.
    Accessibility: `role="status"`, `aria-live="polite"`, `aria-label` matches full label text. When null state, render `<div role="status" aria-live="polite" className="sr-only" />` (UI-SPEC L147).

    **Part B — `frontend/src/i18n/translations.ts` (chip-only subset)** (MOD):

    Insert the AgentStatusChip-related keys from UI-SPEC L420-432 (id locale) and UI-SPEC L435-447 (en locale) — specifically the `agentStatus.*` block (4 labels per locale = 8 entries). The remaining i18n keys (taskPanel.*, askUser.*) ship in Task 2b.

    **Part C — `frontend/src/pages/ChatPage.tsx`** (MOD; or equivalent chat route component):

    Render AgentStatusChip at the top of the chat content column with `sticky top-0 z-10` (UI-SPEC L455). DO NOT add it inside AppLayout.tsx (UI-SPEC L455 explicit prohibition). The component is unconditionally mounted; its internal visibility rule (`agentStatus === null` → sr-only) gates actual rendering.

    DO NOT introduce backdrop-blur on AgentStatusChip.

    Run `cd frontend && npx tsc --noEmit && npm run lint` — must pass with zero errors / warnings introduced by these files.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `frontend/src/components/chat/AgentStatusChip.tsx`
    - `grep -c "backdrop-blur" frontend/src/components/chat/AgentStatusChip.tsx` returns 0 (glass rule honored)
    - `grep -E "setTimeout.*3000" frontend/src/components/chat/AgentStatusChip.tsx` matches (auto-fade timer)
    - `grep -E 'role="status"' frontend/src/components/chat/AgentStatusChip.tsx` matches
    - `grep -c "agentStatus.working" frontend/src/i18n/translations.ts` returns 2 (id + en locales)
    - `grep -c "agentStatus.complete" frontend/src/i18n/translations.ts` returns 2
    - `grep -c "agentStatus.error" frontend/src/i18n/translations.ts` returns 2
    - `grep -c "agentStatus.waiting" frontend/src/i18n/translations.ts` returns 2
    - `grep -E "AgentStatusChip" frontend/src/pages/ChatPage.tsx` matches
    - `grep -E "sticky top-0" frontend/src/pages/ChatPage.tsx` matches
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npm run lint` exits 0 (or with no new errors from these files)
  </acceptance_criteria>
  <done>AgentStatusChip shipped + integrated into ChatPage (sticky top-0 z-10); chip i18n labels present in both locales; type-check + lint pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2b: Build TaskPanel + MessageView question-bubble + remaining i18n + ChatPage TaskPanel integration</name>
  <files>
    frontend/src/components/chat/TaskPanel.tsx,
    frontend/src/components/chat/MessageView.tsx,
    frontend/src/i18n/translations.ts,
    frontend/src/pages/ChatPage.tsx
  </files>
  <read_first>
    - .planning/phases/19-sub-agent-delegation-ask-user-status-recovery/19-UI-SPEC.md (full file — visual contract for TaskPanel + question-bubble)
    - .planning/phases/19-sub-agent-delegation-ask-user-status-recovery/19-PATTERNS.md L703-845 (verbatim TSX excerpts for TaskPanel/TaskCard, MessageView question-bubble)
    - .planning/phases/19-sub-agent-delegation-ask-user-status-recovery/19-PATTERNS.md L953-995 (i18n insertions verbatim)
    - frontend/src/components/chat/PlanPanel.tsx (sidebar slot pattern, StatusIcon precedent at L34-60, collapse pattern L91-102)
    - frontend/src/components/chat/WorkspacePanel.tsx (visibility rule L120, panel shape L154-177)
    - frontend/src/components/chat/SubAgentPanel.tsx (mode/source badge styling L36-46, nested tool-calls font-mono row L52-61)
    - frontend/src/components/chat/MessageView.tsx (assistant bubble path L91-146 — extension target)
    - frontend/src/pages/ChatPage.tsx (or equivalent route component for layout integration)
    - CLAUDE.md (glass rule, base-ui asChild shim — note: TaskPanel uses base-ui tooltip via render prop, NOT asChild)
  </read_first>
  <behavior>
    - TaskPanel renders one card per task_id; visibility = `tasks.size > 0`; collapse toggle works; bg-background (NO backdrop-blur).
    - MessageView shows question-bubble (border-l + MessageCircleQuestion icon) when assistant message has unmatched ask_user tool_call.
    - Remaining 8 i18n keys (taskPanel.* + askUser.*) present in BOTH locales.
    - TaskPanel rendered in ChatPage's panel row to the right of WorkspacePanel.
    - SubAgentPanel.tsx is NOT modified.
    - `cd frontend && npx tsc --noEmit && npm run lint` passes.
  </behavior>
  <action>
    **Part A — `frontend/src/components/chat/TaskPanel.tsx`** (NEW):

    Copy verbatim from PATTERNS.md L703-795 (imports, visibility rule, `<aside>` shell, header, TaskCard, TaskStatusIcon).
    Surface: `bg-background border-l border-border/50 w-72 shrink-0` (NO backdrop-blur per UI-SPEC L161 + CLAUDE.md persistent-panel rule).
    Visibility: `if (tasks.size === 0) return null` (UI-SPEC L159).
    No auto-hide on all-complete (UI-SPEC L200).
    `aria-label={t('taskPanel.title')}`, `role="complementary"`.
    Collapse toggle local `useState`, ChevronDown/ChevronRight icons size 14.
    TaskCard:
      - Row 1: TaskStatusIcon + description (text-sm leading-tight)
      - Row 2: context_files chips (max-w-[120px] truncate, base-ui tooltip via `render` prop NOT `asChild` per CLAUDE.md tooltip shim)
      - Row 3: nested tool calls (pl-4, font-mono text-[11px], tool_call_id text-[10px] opacity-60)
      - Row 4: result preview (line-clamp-2 muted) on complete; error message (line-clamp-2 red) on error
    Status icons mirror PlanPanel exactly: Loader2 animate-spin text-purple-500 (running), CheckCircle2 text-green-500 (complete), AlertCircle text-red-500 (error).

    **Part B — `frontend/src/components/chat/MessageView.tsx`** (MOD):

    Copy `isAskUserQuestion` helper from PATTERNS.md L812-825. Insert variant render block from PATTERNS.md L829-839 AFTER the existing assistant-bubble render (around L116-124), conditional on `askUser.is === true`.

    Render order: normal content first (if any), THEN question-bubble below (UI-SPEC L235).
    `role="note"`, `aria-label={t('askUser.questionBubble.ariaLabel')}`.
    Border: `border-l-[3px] border-primary pl-3 mt-2` (UI-SPEC L234).
    Icon: `MessageCircleQuestion size={16} className="text-primary shrink-0 mt-1" aria-hidden="true"`.

    **Part C — `frontend/src/i18n/translations.ts` (remaining keys)** (MOD):

    Insert the remaining Phase 19 i18n keys (UI-SPEC L420-432 id locale + UI-SPEC L435-447 en locale) — specifically the `taskPanel.*` block and the `askUser.*` block. Place adjacent to the chip keys added in Task 2a, as a single `// Phase 19` comment block.

    **Part D — `frontend/src/pages/ChatPage.tsx`** (MOD; or equivalent chat route component):

    Render TaskPanel in the panel row to the RIGHT of WorkspacePanel — render order: IconRail → ThreadPanel → main content → PlanPanel → WorkspacePanel → TaskPanel (UI-SPEC L157). The component is unconditionally mounted; its internal visibility rule (`tasks.size === 0` → null) gates actual rendering.

    DO NOT modify SubAgentPanel.tsx.
    DO NOT introduce backdrop-blur on TaskPanel.
    DO NOT add a "reply mode" UI for question-bubble — input box stays normal (UI-SPEC L394).

    Run `cd frontend && npx tsc --noEmit && npm run lint` — must pass with zero errors / warnings introduced by these files.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `frontend/src/components/chat/TaskPanel.tsx`
    - `grep -c "MessageCircleQuestion" frontend/src/components/chat/MessageView.tsx` returns at least 1 (question-bubble icon used)
    - `grep -E "isAskUserQuestion" frontend/src/components/chat/MessageView.tsx` matches
    - `grep -E "border-l-\\[3px\\] border-primary" frontend/src/components/chat/MessageView.tsx` matches (D-27 visual)
    - `grep -E "tasks.size === 0" frontend/src/components/chat/TaskPanel.tsx` matches (visibility rule)
    - `grep -E "bg-background" frontend/src/components/chat/TaskPanel.tsx` matches (surface)
    - `grep -c "backdrop-blur" frontend/src/components/chat/TaskPanel.tsx` returns 0 (glass rule honored)
    - **i18n key counts — anchored against full key path, not fragile substrings** (revision-1 Warning 8 fix): `grep -c '"taskPanel.title"' frontend/src/i18n/translations.ts` returns 2 (id + en locales)
    - `grep -c '"askUser.questionBubble.ariaLabel"' frontend/src/i18n/translations.ts` returns 2
    - **TaskPanel title text — anchored against the i18n key, not the substring `"Sub-agen"`** (revision-1 Warning 8 fix): the `taskPanel.title` value MUST start with the substring "Sub-agen" in BOTH locales (id: "Sub-agen Mandiri" or similar Indonesian phrasing per UI-SPEC L425; en: "Sub-agents" per UI-SPEC L440). Verify via: `grep -E '"taskPanel\.title"\s*:\s*"Sub-agen' frontend/src/i18n/translations.ts | wc -l` returns 2 (one per locale; the `[en]:` version uses "Sub-agents" which still matches "Sub-agen" prefix; the `[id]:` version uses the Indonesian translation also starting "Sub-agen"). This anchors against the i18n key path so other unrelated occurrences of "Sub-agen" in the repo do not false-trigger.
    - `git diff --stat frontend/src/components/chat/SubAgentPanel.tsx` shows no changes (UI-SPEC L459)
    - `grep -E "TaskPanel" frontend/src/pages/ChatPage.tsx` matches
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npm run lint` exits 0 (or with no new errors from Phase 19 files)
  </acceptance_criteria>
  <done>TaskPanel + question-bubble + remaining i18n + TaskPanel layout integration shipped; type-check + lint pass; SubAgentPanel untouched; UI-SPEC visual contract honored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server SSE → React reducer | Untrusted SSE event payloads consumed by useChatState; render-time rendering only |
| Render → DOM | All values escaped via React's default text rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-XSS | Information disclosure / Tampering | LLM-generated question text rendered as HTML | mitigate | Question rendered via `<p>{askUser.question}</p>` — React text node (default-escaped). Same for task description and result preview. No raw-HTML rendering APIs are used in any Phase 19 component. Severity if unmitigated: HIGH; severity given React default text-escaping: NONE. |
| T-19-A11Y | Information disclosure (a11y) | Status chip changes invisible to screen readers | mitigate | `role="status" aria-live="polite"` ensures announcements; sr-only container preserved when null state per UI-SPEC L147. |
| T-19-GLASS | UX deviation (CLAUDE.md violation) | Glass added to persistent panel | mitigate | grep gate: `grep -c backdrop-blur` returns 0 in TaskPanel.tsx and AgentStatusChip.tsx. |
</threat_model>

<verification>
1. tsc + lint pass cleanly across all 3 tasks.
2. SSE event types extended; useChatState slices reactive (Task 1).
3. AgentStatusChip + integration shipped (Task 2a).
4. TaskPanel + question-bubble + integration shipped (Task 2b).
5. Visual contract honored: 4 chip variants, glass-free panel, question-bubble with border-l + icon.
6. i18n keys present in both locales (12 × 2 = 24 entries total across 2a + 2b).
7. SubAgentPanel.tsx unmodified (UI-SPEC L459).
8. Layout: AgentStatusChip in ChatPage, NOT AppLayout; TaskPanel rightmost in panel row.
</verification>

<success_criteria>
- [ ] All 8 frontend files modified/created per UI-SPEC
- [ ] tsc + lint pass after each task
- [ ] No backdrop-blur in persistent-panel files
- [ ] All 12 i18n keys × 2 locales present (across 2a + 2b)
- [ ] SubAgentPanel untouched
- [ ] Layout: AgentStatusChip sticky in ChatPage, TaskPanel rightmost
- [ ] T-19-A11Y mitigated; T-19-XSS not introduced; T-19-GLASS verified
- [ ] Each task ≤ 3 files (revision-1 Warning 7 scope-sanity fix)
</success_criteria>

<output>
After completion, create `.planning/phases/19-sub-agent-delegation-ask-user-status-recovery/19-07-SUMMARY.md` capturing:
- Files created / modified per task (Task 1 / 2a / 2b breakdown)
- Confirmation: SubAgentPanel.tsx untouched
- i18n key count: 12 × 2 = 24
- Lines added per file (approximate)
- Visual contract sign-off note
</output>
