---
phase: 19
plan: 10
type: execute
wave: 5
depends_on: [19-07]
files_modified:
  - frontend/src/components/chat/AgentStatusChip.test.tsx
  - frontend/src/components/chat/TaskPanel.test.tsx
  - frontend/src/components/chat/MessageView.test.tsx
autonomous: true
requirements: [TASK-07, ASK-02, STATUS-01]
security:
  asvs_level: 1
  block_on: high
must_haves:
  truths:
    - "AgentStatusChip Vitest covers all 4 visual states + null state + auto-fade timer + a11y attributes [satisfies STATUS-01 / D-26]"
    - "TaskPanel Vitest covers visibility rule + collapse + status icons + context_files chips + nested tool calls + complete/error result preview [satisfies TASK-07 / D-25]"
    - "MessageView question-bubble variant Vitest covers detection logic (ask_user with no matching tool_result) + visual variant + a11y [satisfies ASK-02 / D-27]"
    - "All Vitest suites use vitest 3.2 + @testing-library/react patterns from CodeExecutionPanel.test.tsx precedent [satisfies v1.2 D-P16-02]"
  artifacts:
    - path: "frontend/src/components/chat/AgentStatusChip.test.tsx"
      provides: "Vitest 3.2 component tests for AgentStatusChip"
      contains: "renders nothing when agentStatus is null"
    - path: "frontend/src/components/chat/TaskPanel.test.tsx"
      provides: "Vitest 3.2 component tests for TaskPanel"
      contains: "renders nothing when tasks size is zero"
    - path: "frontend/src/components/chat/MessageView.test.tsx"
      provides: "Vitest 3.2 question-bubble variant tests (or addition to existing test file if present)"
      contains: "isAskUserQuestion"
  key_links:
    - from: "frontend/src/components/chat/AgentStatusChip.test.tsx"
      to: "frontend/src/components/chat/CodeExecutionPanel.test.tsx"
      via: "Vitest 3.2 co-located convention from v1.2 D-P16-02"
      pattern: "import.*vitest"
---

<objective>
Vitest component tests for the three Phase 19 frontend additions (AgentStatusChip, TaskPanel, MessageView question-bubble variant).

Purpose: 19-07 shipped UI components verified only by tsc + lint. This plan delivers behavioral tests using the Vitest 3.2 co-located convention bootstrapped in v1.2 (D-P16-02). Without these tests, frontend regressions land silently.

Output: Three test files with cumulative ~22 cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-sub-agent-delegation-ask-user-status-recovery/19-CONTEXT.md
@.planning/phases/19-sub-agent-delegation-ask-user-status-recovery/19-PATTERNS.md
@.planning/phases/19-sub-agent-delegation-ask-user-status-recovery/19-UI-SPEC.md
@.planning/phases/19-sub-agent-delegation-ask-user-status-recovery/19-07-SUMMARY.md

@frontend/src/components/chat/AgentStatusChip.tsx
@frontend/src/components/chat/TaskPanel.tsx
@frontend/src/components/chat/MessageView.tsx
@frontend/src/components/chat/CodeExecutionPanel.test.tsx
@frontend/src/components/chat/WorkspacePanel.test.tsx
@frontend/src/i18n/I18nContext.tsx
@frontend/src/contexts/ChatContext.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write AgentStatusChip.test.tsx</name>
  <files>frontend/src/components/chat/AgentStatusChip.test.tsx</files>
  <read_first>
    - frontend/src/components/chat/CodeExecutionPanel.test.tsx (full — primary Vitest 3.2 analog)
    - frontend/src/components/chat/AgentStatusChip.tsx (the component under test, from 19-07)
    - frontend/src/contexts/ChatContext.tsx (for ChatContextProvider mock pattern)
    - frontend/src/i18n/I18nContext.tsx (for I18nProvider wrapper)
    - .planning/phases/19-sub-agent-delegation-ask-user-status-recovery/19-PATTERNS.md L1086-1092 (test list spec)
    - .planning/phases/19-sub-agent-delegation-ask-user-status-recovery/19-UI-SPEC.md (visual contract for assertions)
  </read_first>
  <behavior>
    - Test 1: `renders nothing visible when agentStatus is null` — only sr-only aria-live container exists in DOM; no chip text shown.
    - Test 2: `renders pulsing dot and "Agent working" copy on working` — assert pulsing dot element (`bg-primary animate-pulse`) + label "Agent working" (en) or "Agen sedang bekerja" (id).
    - Test 3: `renders MessageCircleQuestion icon on waiting_for_user` — assert lucide icon present + label "Agent waiting for your reply" / "Agen menunggu balasan Anda".
    - Test 4: `renders CheckCircle2 and auto-fades after 3000ms on complete` — use `vi.useFakeTimers()`; render with status='complete'; advance timers 3000ms; assert setAgentStatus(null) called. (Mock setAgentStatus via stubbed context.)
    - Test 5: `renders AlertCircle on error and persists (no auto-fade)` — render with status='error'; advance timers 5000ms; assert chip still present, setAgentStatus NOT called.
    - Test 6: `has role=status and aria-live=polite on container` — assert ARIA attributes present in both visible and null states.
  </behavior>
  <action>
    Create `frontend/src/components/chat/AgentStatusChip.test.tsx`.

    Follow the import + render-helper pattern from `CodeExecutionPanel.test.tsx` L1-35 (PATTERNS.md L1070-1083):
    ```tsx
    import { describe, it, expect, vi, beforeEach } from 'vitest'
    import { render, screen } from '@testing-library/react'
    import { I18nProvider } from '@/i18n/I18nContext'
    import { AgentStatusChip } from './AgentStatusChip'

    // Mock useChatContext to return controllable agentStatus + setAgentStatus
    vi.mock('@/contexts/ChatContext', () => ({
      useChatContext: vi.fn(),
    }))

    beforeEach(() => {
      vi.clearAllMocks()
      vi.useFakeTimers()
    })
    ```

    For the auto-fade test (Test 4): use `vi.advanceTimersByTime(3000)`; assert mock `setAgentStatus` called with `null`.

    Wrap render with `<I18nProvider locale="en">`; verify both ID and EN labels by also testing `<I18nProvider locale="id">` for Test 2 only.

    Use `screen.getByRole('status')`, `screen.getByText('Agent working')`, `screen.queryByText(...)`, `screen.getByLabelText(...)` etc.

    For icon assertions, use `container.querySelector('[aria-hidden="true"]')` or `data-testid` attributes (since lucide icons render as SVG).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/components/chat/AgentStatusChip.test.tsx 2>&amp;1 | tail -5 | grep -E "6 passed|Tests.*6 passed"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `frontend/src/components/chat/AgentStatusChip.test.tsx`
    - `grep -c "it\\(" frontend/src/components/chat/AgentStatusChip.test.tsx` returns at least 6
    - `grep -E "vi\\.useFakeTimers|advanceTimersByTime" frontend/src/components/chat/AgentStatusChip.test.tsx` matches
    - `grep -E "role.*status|aria-live" frontend/src/components/chat/AgentStatusChip.test.tsx` matches
    - `npx vitest run src/components/chat/AgentStatusChip.test.tsx` reports `6 passed`
  </acceptance_criteria>
  <done>AgentStatusChip Vitest committed; 6 tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write TaskPanel.test.tsx</name>
  <files>frontend/src/components/chat/TaskPanel.test.tsx</files>
  <read_first>
    - frontend/src/components/chat/CodeExecutionPanel.test.tsx (Vitest 3.2 analog)
    - frontend/src/components/chat/WorkspacePanel.test.tsx (closer ChatContext-integration analog if present)
    - frontend/src/components/chat/TaskPanel.tsx (component under test, from 19-07)
    - .planning/phases/19-sub-agent-delegation-ask-user-status-recovery/19-PATTERNS.md L1094-1103 (test list spec)
    - .planning/phases/19-sub-agent-delegation-ask-user-status-recovery/19-UI-SPEC.md "TaskPanel" section (full visual contract)
  </read_first>
  <behavior>
    - Test 1: `renders nothing when tasks size is zero` — `tasks: new Map()` → no `<aside>` rendered.
    - Test 2: `renders one card per task entry` — Map with 3 tasks → 3 task-card list items.
    - Test 3: `running task shows Loader2 spinner with animate-spin and text-purple-500` — assert spinner classes.
    - Test 4: `complete task shows CheckCircle2 and result preview with line-clamp-2` — assert green checkmark + truncated result.
    - Test 5: `error task shows AlertCircle and error detail in red` — assert red alert + error.detail text.
    - Test 6: `context_files render as truncated chips with title attribute for tooltip` — chip class `max-w-[120px] truncate`; `title` attribute = full path.
    - Test 7: `nested tool calls list renders with font-mono text-[11px]` — assert font-mono class on each row.
    - Test 8: `collapse button toggles content area` — click collapse → content hidden; click again → visible.
    - Test 9: `has role=complementary and aria-label` — accessibility check.
  </behavior>
  <action>
    Create `frontend/src/components/chat/TaskPanel.test.tsx` mirroring AgentStatusChip.test.tsx import shape.

    Mock `useChatContext` to return controllable `tasks: Map<string, TaskState>` slice.

    For mixed-state tasks, build a Map with three entries: one running, one complete (with result), one error (with error detail).

    Use `userEvent.click()` for the collapse button toggle test.

    Wrap with `<I18nProvider locale="en">`.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/components/chat/TaskPanel.test.tsx 2>&amp;1 | tail -5 | grep -E "9 passed|Tests.*9 passed"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `frontend/src/components/chat/TaskPanel.test.tsx`
    - `grep -c "it\\(" frontend/src/components/chat/TaskPanel.test.tsx` returns at least 9
    - `grep -E "userEvent" frontend/src/components/chat/TaskPanel.test.tsx` matches (collapse toggle test)
    - `grep -E "role.*complementary" frontend/src/components/chat/TaskPanel.test.tsx` matches
    - `npx vitest run src/components/chat/TaskPanel.test.tsx` reports `9 passed`
  </acceptance_criteria>
  <done>TaskPanel Vitest committed; 9 tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write MessageView question-bubble variant tests</name>
  <files>frontend/src/components/chat/MessageView.test.tsx</files>
  <read_first>
    - frontend/src/components/chat/MessageView.tsx (modified in 19-07; isAskUserQuestion helper at the top of file)
    - frontend/src/components/chat/CodeExecutionPanel.test.tsx (Vitest analog)
    - .planning/phases/19-sub-agent-delegation-ask-user-status-recovery/19-UI-SPEC.md "Question-bubble variant" section
    - .planning/phases/19-sub-agent-delegation-ask-user-status-recovery/19-PATTERNS.md L806-846 (verbatim TSX excerpt)
  </read_first>
  <behavior>
    - Test 1: `renders normal assistant bubble when message has no tool_calls` — no question-bubble rendered.
    - Test 2: `renders normal assistant bubble when ask_user tool_call has matching tool_result` — pair complete; question-bubble NOT rendered (D-27 detection logic).
    - Test 3: `renders question-bubble when ask_user tool_call has no matching tool_result` — question-bubble rendered with the question text from tool_call.input.question.
    - Test 4: `question-bubble has border-l-[3px] border-primary` — visual contract verified.
    - Test 5: `question-bubble has role=note and aria-label` — accessibility (UI-SPEC L243-245).
    - Test 6: `MessageCircleQuestion icon present in question-bubble` — icon rendered.
    - Test 7: `assistant content rendered before question-bubble when both exist` — DOM order: content first, then question-bubble (UI-SPEC L235).
  </behavior>
  <action>
    Create `frontend/src/components/chat/MessageView.test.tsx` (or extend an existing one if present — check first via `ls frontend/src/components/chat/MessageView.test.tsx 2>/dev/null`).

    Build sample messages with:
    - tool_calls = [] → Test 1 input
    - tool_calls = [{tool: 'ask_user', tool_call_id: 'X', input: {question: 'Q?'}}], toolResults = [{tool: 'ask_user', tool_call_id: 'X', ...}] → Test 2 input
    - tool_calls = [{tool: 'ask_user', tool_call_id: 'Y', input: {question: 'Q?'}}], toolResults = [] → Test 3 input
    - Test 7 message has both `content: "Some intro text"` AND ask_user tool_call without matching result.

    Wrap with `<I18nProvider locale="en">`.

    Use `screen.queryByRole('note')`, `screen.queryByText('Q?')`, `container.querySelector(...)` for class checks.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/components/chat/MessageView.test.tsx 2>&amp;1 | tail -5 | grep -E "7 passed|Tests.*7 passed"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `frontend/src/components/chat/MessageView.test.tsx`
    - `grep -c "it\\(" frontend/src/components/chat/MessageView.test.tsx` returns at least 7
    - `grep -E "ask_user" frontend/src/components/chat/MessageView.test.tsx` matches
    - `grep -E "border-l-\\[3px\\]" frontend/src/components/chat/MessageView.test.tsx` matches
    - `grep -E "role.*note" frontend/src/components/chat/MessageView.test.tsx` matches
    - `npx vitest run src/components/chat/MessageView.test.tsx` reports `7 passed`
  </acceptance_criteria>
  <done>MessageView question-bubble Vitest committed; 7 tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → component render | Mock useChatContext slices; verify visual + a11y contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-A11Y | Information disclosure (a11y) | Status chip / panel changes invisible to screen readers | mitigate | Tests 6 (chip), 9 (panel), 5 (question-bubble) verify role + aria-live + aria-label attributes. |
| T-19-VISUAL | UX deviation | Visual contract drift undetected | mitigate | Multiple tests assert specific Tailwind class strings (`border-l-[3px] border-primary`, `text-purple-500 animate-spin`, `bg-primary animate-pulse`) per UI-SPEC. |
</threat_model>

<verification>
1. All 22 Vitest tests pass cumulatively (6 + 9 + 7).
2. Visual contract assertions enforce class strings from UI-SPEC.
3. A11y attributes verified.
4. Auto-fade timer behavior verified with fake timers.
5. SubAgentPanel.tsx still untouched (no SubAgentPanel.test.tsx changes either — coexistence preserved).
</verification>

<success_criteria>
- [ ] All 22 Vitest tests pass (6 + 9 + 7)
- [ ] Tests follow CodeExecutionPanel.test.tsx pattern (Vitest 3.2)
- [ ] Visual + a11y contract verified
- [ ] Auto-fade behavior verified with fake timers
</success_criteria>

<output>
After completion, create `.planning/phases/19-sub-agent-delegation-ask-user-status-recovery/19-10-SUMMARY.md` capturing:
- Test counts per file (6 / 9 / 7 = 22 total)
- Cumulative pass status
- Phase 19 totals: backend tests + frontend tests + E2E tests aggregate count
</output>
